# Cursor执行指令｜FP-20260916-R3.1

在当前工程上补充修订资金、产权**下半区**及对应场景配置。R3.1 优先于 R3 中冲突的页面及场景展示要求。

## 范围

- 只改资金、产权下半区和场景配置。
- 上半区指标趋势、综合总览、既有正确计算、首批启用规则、历史评估与整改一律保持。
- 不批量启用 80 条，不伪造执行结果，不从命中记录或已启用子场景反向生成一级目录。

## 必须实现

1. 资金、产权下半区各合为一张「场景执行情况」卡片，删除独立导航大卡。
2. 默认「全部专题」，分别完整展示原始 11 项、10 项一级监管场景；每项可展开查看原有子场景及真实状态。
3. 无命中、无业务、暂未开展监测的一级项仍须可查。
4. 产权只有选择「产权交易」时才出现经济行为下拉和对应肩形流程；其他专题隐藏交易筛选及流程。
5. 流程箭头不再重复堆「停留、未关闭」等统计。
6. 规范旧泛称分组及「补充监管场景」标签；官方 80 条按其执行能力标注。
7. 按 21 项逐条对照验收，提供完整目录截图、交易与非交易状态截图，沿用 GitHub Pages `/-/` 发布。

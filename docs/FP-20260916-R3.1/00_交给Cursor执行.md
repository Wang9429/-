# Cursor执行指令｜FP-20260916-R3.1

在当前工程上增量修订资金、产权下半区、对应场景配置，以及产权上半区指标显示。本文件及 `01_资金产权场景完整覆盖_R3.1.md` 优先于 R3 中冲突的页面、场景展示及产权趋势要求。

## 范围

- 资金、产权下半区导航/场景展示、场景配置归属。
- 产权上半区：四卡不常显公式，首页和详情均取消趋势图。
- 资金指标与趋势、组织树、综合总览、既有正确计算、首批启用规则、历史评估与整改一律保持。
- 不批量启用 80 条，不伪造执行结果，不从命中记录或已启用子场景反向生成一级目录。

## 必须实现

1. 产权四卡只保留名称、当前值、单位、必要业务分类和详情入口；完整口径经「计算依据」查看。
2. 产权首页及详情取消全部趋势图；配置锁定后刷新不能恢复。股权图、组织穿透保留。
3. 资金、产权下半区各合为一张「场景执行情况」卡片，删除独立导航大卡。
4. 默认「全部专题」，分别完整展示原始 11 项、10 项一级监管场景；每项可展开查看原有子场景及真实状态。
5. 产权只有选择「产权交易」时才出现经济行为下拉；选定具体经济行为后再显示肩形流程。其他专题隐藏交易筛选及流程。
6. 流程箭头不再重复堆「停留、未关闭」等统计。
7. 规范旧泛称分组及「补充监管场景」标签；未启用 / 未具备运行条件 / 未开展监测分列，不伪造 0 命中。
8. 按 12 项验收实点，输出资金 11/11、产权 10/10，沿用 GitHub Pages `/-/` 发布。

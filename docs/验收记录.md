# 验收记录（V1.6）

记录日期：2026-09-15。原型功能完成状态与真实系统条件核实状态分开。未连接生产接口、未做正式登录与模型推理。

## 已验证（计算与口径，`npm run verify`）

对照 `demo_seed.json` 的 `expected_results` / `expected_stage_results_v1_2`：

- 总部固定资产投资计划执行率 84%（8400÷10000）
- FA-P001 80%、FA-P003 90%
- FA-P001 预计完工投资偏差 18%
- 建设实施 FA-S20 五数 2/1/1/0/0；运营及后评价 FA-S29 五数 2/1/1/0/1
- 恢复种子未关闭 8、高风险 4、逾期整改 1；R09 已排除
- 账户 6632 / 受限 314.4 / 可用 6317.6

覆盖规划候选（358）不进应评估分母；已完成观察（31）进入五数。

## 本轮增量已落地（页面与交互）

| 项 | 结果 | 说明 |
|---|---|---|
| 侧栏 248px、系统配置入口、规范文案 | 通过（代码） | 业务标题/按钮不显示 P71–P79；不出现「重置演示数据」 |
| 综合总览 84/8/4/1 | 通过（代码+口径） | 指标打开组织穿透 |
| 计算依据按指标绑定 | 通过（代码） | 执行率与 EAC 偏差各自公式 |
| 六领域首页 KPI 4–6 张、资金专题、国际化事件 | 沿用并授权过滤 | 未删领域 |
| AI 预置分析 | 通过（代码） | 未连接模型；越权任务提示超范围 |
| R07 合成材料显式采用、独立复核 | 通过（代码） | 草稿不自动当证据；自审禁用 |
| 配置六 Tab、试算/发布、双重置 | 通过（代码） | 配置与业务分键保存 |
| 单位 A/B 隔离、配置管理员无业务数据 | 通过（代码） | 查询链求交授权组织/对象 |
| 实施调研对应表 | 已提供文档 | 来源系统待科技信息部核实 |

## 未宣称 / 待调研

- 真实组织目录、单点登录、服务端鉴权
- 投资计划/完成、付款批准与实付的真实接口字段与更新频率
- 外部行情订阅与模型服务
- 全公司覆盖规模或上线成效

## 第二批（范围、期间、覆盖、权限）

对应检查报告 R04—R07、R14。在 `4ab8161` 第一批 UI 成果上修复已有机制的执行遗漏，不新增审批层级或基层填报。

| 路径 | 结果 |
|---|---|
| 总部含下级打开执行率抽屉 | 背景与抽屉初始值均为 84% |
| 总部仅本级打开执行率抽屉 | 背景与抽屉均为无业务，不再自动变成含下级 84% |
| 单位 B | 执行率 90%；未关闭事项显示 0；Tab/AI/导出/试算不出现单位 A 事实 |
| 总部管理人员A（只读） | 不能采用材料、督办、发布规则或重置 |
| 配置管理员A | 可看配置定义；试算仅独立测试输入，不读总部业务金额 |
| 切换期间 | 快照指标显示「该期间数据未覆盖」；资金/工程收付款按交易日期过滤；H1 仍为 84/80/90 |
| FA-S02、FA-S21 | 未确认候选不进入业务应评估分母 |
| R07 整改与独立复核 | 有权人员仍可完成；跨领域同步；1200/800/2000 不变 |

`npm run verify`：原口径 48 项 + 第二批 24 项，共 72 项通过。

## 环境限制

远程环境内部 `localhost` 不能当作外部用户入口。本轮沿用已授权预览转发；不为获得链接新建外部隧道。

UI 缩放：CDP 页面缩放模拟已检查，真实浏览器菜单 125% 仍待验证。

## 综合总览排版与事项清单浮层（本轮）

范围：只改综合总览 KPI / 领域卡 / 表格排版，以及事项数字点击后的清单浮层。未改股权默认配置、矩阵统计口径、事件期间筛选、规则执行绑定。

| 项 | 结果 |
|---|---|
| 顶部 KPI 标题、主数字、辅助信息分槽对齐 | 已改 `KpiCard`；金额拆成「完成 n 万元」「同期计划 n 万元」两段，万元不拆行 |
| 1440 四列 / 1280 两列 | 沿用 `.reg-kpis` 既有断点 |
| 六领域卡标题、指标、「进入领域」网格对齐 | 股权无首页指标时显示空态，不写 62.5% |
| 单位风险分布列宽与表头「合计（去重）」 | 表内横滚，不改 0 / 无业务 / 未评估 口径 |
| 1280 待复核与近期事件上下排列；宽屏顶对齐、空表不拉高 | `.reg-split` 在 1440 起两列 `align-items: start` |
| 事项数字打开清单 Modal，不再追加页底 | 关闭详情回到清单；关闭清单恢复滚动位置 |

### 后续功能纠错（本轮未执行）

- 股权 EQ-I11 默认停用/仅目录，总览股权卡无 62.5% 主指标
- 事项清单仍按点击时 ID 展示；切换组织或期间后清单可能陈旧
- 风险矩阵仍用整单位 `hasBusiness`，「本次监测未发现异常」与未评估未分开
- 近期事件未按所选期间过滤，点击不定位到国际化对应事件
- C01 / C02 规则执行绑定与发布版本
- 真实浏览器 125% 缩放未在本环境验证

## 全站 UI 统一（本轮）

以综合总览已确认样式为基准，优先改共用组件后铺到各业务页、工作台、配置 Tab 与浮层。未改指标公式、金额、权限、场景映射和办理逻辑。

| 项 | 处理 |
|---|---|
| KPI 分槽对齐、单位不拆行、百分号一次 | 沿用 `KpiCard`；领域首页改用 `.reg-kpis-domain` |
| 筛选区紧凑换行 | `FilterBar` 收窄宽度，`PageHeader` 与筛选同行可折行 |
| 流程条紧随指标下方 | `DomainPage` 先流程后重点关注，便于 1440 首屏看到核心指标和肩形箭头 |
| 流程名称完整 | `ChevronFlow` 按名称加宽，容器内横滚 |
| 长表分页、名称列宽度、金额右对齐 | `DataTable` 增加 `pageSize`/`minWidth`；分页只切展示行 |
| 配置双列表 1280 上下排、空表不拉高 | `.reg-split` |
| 浮层层级与 AI 入口 | 沿用现有 overlay 栈与顶栏/标题栏 AI 入口 |

已知业务逻辑问题仍见上一节「后续功能纠错」，本轮未处理。

## 全站 UI 收尾（本轮）

基于 `8eb4ca5`，只处理配置操作区、页面滚动策略与遗漏独立页样式。未改股权默认配置、清单 ID、矩阵口径、事件筛选、C01／C02。

| 项 | 处理 |
|---|---|
| 配置操作列 | `DataTable` 支持 `sticky: "right"`；查看／编辑／启用停用固定在表右，不随内容横滚离开视口 |
| 配置双列表 | 场景 Tab 改为 `.reg-settings-pair` 始终上下排，1440 半栏不再裁切停用按钮 |
| 页面滚动 | 未缩小 KPI／按钮／流程条；1280 固定资产 7 张指标下移流程条仍可滚动 |
| 对象档案独立页 | `/object/[id]` 不再用抽屉盖住侧栏；沿用 `PageHeader`、表格分页与全站表样式 |
| 场景规则库独立页 | 顶栏恢复入口；页内改用 `PageHeader`、长表分页与名称列宽度 |
| 真实业务状态 | 未发布／未执行等状态照常展示 |

### 后续功能纠错（本轮仍未执行）

- 股权 EQ-I11 默认停用/仅目录，总览股权卡无 62.5% 主指标
- 事项清单仍按点击时 ID 展示；切换组织或期间后清单可能陈旧
- 风险矩阵仍用整单位 `hasBusiness`，「本次监测未发现异常」与未评估未分开
- 近期事件未按所选期间过滤，点击不定位到国际化对应事件
- C01 / C02 规则执行绑定与发布版本
- 真实浏览器 125% 缩放未在本环境验证

## 三项展示收尾（本轮）

保留 `cae0637` 及此前已统一的侧栏、配色、卡片、表格、流程和浮层。只改正单位转换、状态重复拼接与场景开发文案。未改指标配置、取数口径、权限、流程、办理或发布逻辑。

| 项 | 处理 |
|---|---|
| 资金底部合计 | 账户原币×汇率为元，展示前折万元，与 KPI 同口径按当前筛选汇总 |
| 超概关注规则命中 | `formatKpiCaption` 使「关注规则未发布，未执行」只出现一次；主数值仍为 — |
| 场景文案 | 「补充监管场景」「监管指标」；列表改为「查看依据」；平台行为/排除操作写入实施说明 10.4 |

股权指标、清单陈旧、矩阵状态、事件筛选与跳转、C01／C02 仍未执行。

## 综合总览监管主体全景（本轮）

范围：只改综合总览结构、首页统计口径与必要联动；不改各领域首页业务布局，不启动固定资产等领域改造，不处理 C01／C02、真实接口或 AI 接入。

业需与计算依据写入：

- `docs/系统原型定位与实施调研说明_V1.6.md` 第 2 节
- `docs/海油工程穿透式监管平台_完整业需_V1.6.md` 第 6 章、第 16.2 节、第 23.2 节及 AC101
- 实现口径：`lib/overview.ts`

| 项 | 处理 |
|---|---|
| 两主区块 | 监管主体全景 + 专项领域监管概况；去掉摘要、待复核表、外部事件表、单位×领域矩阵、首页重点事项表 |
| 四项总体数字 | 纳管单位（组织 ID 去重，总部在范围内计入）、在管项目（FA+EQ+ENG 去重）、高风险责任单位（实际责任单位 ID）、未关闭高风险（附逾期高风险） |
| 主体层级图 | 根为海油工程总部；默认总部+直属二级。选中后只展示该单位及其直属下级，上级留在路径；点击选中并同步筛选 |
| 领域卡 | 每领域两项主指标 + 短异常标签 + 进入领域；FA 计划执行率回到固定资产卡 |
| 启停 | EQ-I11 / EQ-I08 保持未启用，对应空态；不批量改目录 |
| 浮层 | 筛选或身份变化关闭旧清单/抽屉；抽屉钻取不改背景组织 |

固定资产领域业务改造、矩阵 0/无业务/未评估纠错、事件期间筛选、C01／C02 本轮仍未执行。

### 本轮交互核对（非路由 200）

提交 `797db3b`。`npm run verify` 85 项通过。Chrome 实机（1440/1280，清本地状态后）：

| 路径 | 结果 |
|---|---|
| 总部默认 | 纳管单位 6（含总部）、在管项目 6、高风险责任单位 2、未关闭高风险 4／逾期 1；两主区块名称正确；无矩阵/待复核/事件表；股权偏差「指标未启用」 |
| 点二级单位 A 并展开三级 | 筛选变为下属二级单位 A（含下级）；单位 3／项目 4；路径与返回总部可用；A1、境外分支 A2 出现；B/C 无虚构三级 |
| 进入固定资产再回总览 | 领域页与返回后均保留下属二级单位 A（含下级）及原期间 |
| 高风险数字 → 清单 → 详情 | 清单 4 条与数字一致；详情叠在清单上 |
| 指标抽屉钻取 A1 | 背景筛选仍为单位 A；抽屉内 A1 执行率 80% |
| 切换期间、返回总部、切配置管理员 | 旧事项清单关闭 |
| 1440 / 1280 横溢 | `scrollWidth===clientWidth`，无整页横向溢出 |

未验证：真实浏览器菜单 125% 缩放；未开始固定资产等领域业务改造。

## 综合总览口径改版与扩容（本轮）

提交 `36e8660`（实现主体 `912f76e`，本提交补整改卡标题可点与 Chrome 交互核对脚本）。`npm run verify` 口径脚本 93 项通过，**不以该项替代页面点击**。

Chrome（`scripts/verify-overview-ui.mjs`，1440/1280，清 `localStorage` 后，生产服务 `http://127.0.0.1:43917`）**33 项交互全部通过**。

总部默认可见数字（2026 年上半年 / 截至 2026-06-30 / 总部含下级）：

| 指标 | 页面数字 | 对应清单 |
|---|---|---|
| 纳管单位数 | 22 家 | 清单 22 家；不含总部财务部、单位A项目部 |
| 纳管项目数 | 81 个 | 清单 81 条，可按固定资产/股权/工程筛选 |
| 规则命中涉及单位数 | 7 家 | 清单 7 家，均为对象实际归属单位 |
| 未关闭整改事项数 | 8 件 | 清单 8 条；本期完成 1、逾期 2 |

| 验收项 | 结果 | 说明 |
|---|---|---|
| 停用 FA-I06 后首页隐藏，再启用恢复 | 通过（点击） | 固定资产投资卡不再出现投资完成额、投资计划执行率；启用后两项均恢复 |
| 总部 → 单位 A → A1 → 关联对象 | 通过（点击） | 筛选变为下属二级单位A（含下级）；A1/A2 并排；末级打开基地能力提升项目档案 |
| 顶部数字与清单条数 | 通过 | 22/81/7/8 与浮层「共 n 家/条」一致 |
| 命中单位不重复祖先 | 通过 | 7 家为 A1、D、D海工、E、E安装、F、A2，无总部、无单位 A 汇总节点 |
| 命中规则不按运行次数 | 通过 | 监测浮层写明「不按运行次数累加」；对象按类型分列 |
| 待核查不计入未关闭整改 | 通过 | 8 条均为整改中或待复核，无待核查、无已排除 |
| 规则停用后历史仍可查 | 通过 | 停用 FA-R20-01 后未关闭整改仍 8 件；命中层仍见 FA-R20-01 |
| 切换单位 A 身份 / 配置管理员 | 通过 | 单位 A 看不到下属二级单位 D；配置管理员见无业务数据权限 |
| 1440 / 1280 | 通过 | `scrollWidth===clientWidth`；所属单位下属区 `max-height:420px` 滚动，不铺满首页 |
| 无介绍摘要、名词解释、演示字样、未启用指标占位 | 通过 | 股权「现金回报目标偏差」未出现；无「指标未启用」 |

截图：`overview_hq_1440_36e8660.png`、`overview_unit_a_1440_36e8660.png`、`overview_monitoring_list_1440_36e8660.png`、`overview_rectification_detail_1440_36e8660.png`、`overview_hq_1280_36e8660.png`。

### 本轮未完成 / 未在浏览器再点一遍

- 未开始固定资产投资等领域首页业务改造，未做 C01／C02。
- 截至日筛选仍固定 2026-06-30；`statusAtAsOf` 已按当时状态还原，但界面没有第二个历史截至日可点。
- 本轮浏览器未再现场办理「复核关闭」一条新事项；本期完成 1 件来自种子已关闭事项（R11），口径脚本覆盖重开不计入完成数。
- 单位清单「纳管项目」列按该单位本级对象计，单位卡按该节点监管范围计；点击单位卡项目数字已验证只开清单、不切换组织，未逐张对每张卡的数字与清单条数。
- 真实浏览器菜单 125% 缩放仍未验证。
- 事项详情抽屉内仍有既有「Demo 口径」提示，业务总览首页未新增「演示」字样。

## 综合总览四项收尾（本轮）

提交以 `6af047b` 为实现主体。Chrome 交互脚本 `scripts/verify-overview-wrap.mjs` **23 项全部通过**，不以口径脚本或种子已关闭事项替代。验证后已清除本地业务状态。截图包：预览根路径 `/overview-wrap-screenshots.zip`。

| 项 | 实际结果 |
|---|---|
| 1. 停用投资计划执行率 | 配置项 **FA-I06**。完成额是该指标分子，不是独立首页指标。停用后固定资产卡两项均消失，股权/资金等其他领域指标仍在；未补位。重新启用后两项恢复。 |
| 2. 单位卡与清单范围 | 总部含下级 **81=81**；单位A含下级 **13=13**（标题含下级，清单含 A/A1/A2 项目）；A 本级 **4=4**（列名本级纳管项目数）；末级 A1 **3=3**（仅本级）；无三级的单位 B **6=6**。分页「共 n 条」与卡片一致。 |
| 2b. 命中涉及单位 | 按对象实际归属。下属二级单位 A 因 ENG-P016 直接命中计入；总部无直接命中对象故不计入；A1 仍计入。未写死排除全部非末级。现为 **8 家**。 |
| 3. 历史截至日 2026-05-15 | 可切换。未关闭整改 **3**（含 R11，状态还原为整改中）；6 月末为 8。命中涉及单位当时记为 0（评估均在 6-30）；本轮已改为「—／未开展监测」，见下一节。金额指标显示「该截至日数据未覆盖」，不出现 84.555%。旧清单关闭。切回 6-30 后数字恢复。 |
| 4. 整改复核关闭 R02 | 总部监管人员A 提交整改 → 总部复核人员B 复核通过。未关闭 **8→7**，本期完成 **1→2**，逾期 **2→1**，清单去掉「重大装备…」，固定资产卡未关闭整改 **1→0**。随后重置业务，恢复 8/1/2。 |

未完成：未开始其他领域首页改版；未做 C01／C02；未改全站视觉；真实浏览器 125% 缩放未验证。5 月 15 日没有独立财务快照，金额类指标按未覆盖展示，这是有意为之。

## 综合总览显示修正（本轮收尾）

结构、扩容种子与已验证交互保持上一轮。仅两处显示修正。默认预览截至日仍为 **2026-06-30**。本轮只核对该默认日与历史日 **2026-05-15**，不再扩大为整改闭环或全量交互。实现提交 `1cf7928`，计算依据首屏标注 `354959a`。`npm run verify` **113** 项通过。Chrome 脚本 `scripts/verify-overview-display.mjs` **19 项全部通过**。

| 项 | 实际结果 |
|---|---|
| 1. 完成额分子说明离开首页 | 固定资产卡以**投资计划执行率**为主指标（84.55%）、**投资完成额**为辅助业务字段（10,400 万元），二者仍共用 FA-I06 开关。首页不再常显「完成额为执行率分子」。打开执行率抽屉后可见「分子（投资完成额）」及口径说明；配置口径说明仍写明共用开关。 |
| 2. 历史截至日监测空态 | 2026-05-15：规则命中涉及单位数 **—／未开展监测**。单位卡命中规则 **—**；领域卡「命中规则 —｜未开展监测」；点击清单写明没有适用监测执行记录。未关闭整改仍为 **3**（含 R11，状态还原为整改中），不为对齐监测数字而补造历史评估。切回 6-30 后命中单位恢复 **8 家**（当前范围为部分完成监测，显示已完成部分的命中）。 |

未完成：同上一节。综合总览本轮收尾。

## 综合总览—专项领域指标穿透（本轮）

范围：共用指标抽屉的范围与联动；不改各领域首页业务布局，不启动固定资产等领域改造，不处理 C01／C02。默认截至日仍为 **2026-06-30**。

业需同步：

- `docs/海油工程穿透式监管平台_完整业需_V1.6.md` 第 4.2 / 4.2.1 / 6.3 / 6.4 / 23.2.2 节及 AC109
- `docs/系统原型定位与实施调研说明_V1.6.md` 第 2.2 / 2.3 节
- `AGENTS.md` 首页与指标浮层；`docs/操作说明.md` 路径一

实现提交 **`e37b0ee`**。`npm run verify` **142** 项通过（含本轮抽屉范围断言）。Chrome 生产构建脚本 `scripts/verify-indicator-drawer.mjs` **41 项全部通过**，不以口径脚本替代点击。

| 验收项 | 实际点击结果 |
|---|---|
| 总览与固定资产打开同一执行率 | 均为海油工程总部、期间 2026-01-01~2026-06-30、截至 2026-06-30、**84.55%** |
| 总览取消切换指标 | 投资计划执行率抽屉无整排切换；不出现股权等其他领域指标 |
| 进入领域 | 「进入领域」到固定资产首页，单位 A（含下级）筛选保留 |
| 总部→A→A1→项目 | 抽屉内逐层到基地能力提升项目 **80%**（4800/6000）；背景筛选仍为 `ORG-HQ::desc` |
| 单位 A 不能到单位 B | 树以单位 A 为根，可见 A1/A2/项目部；总部仅路径展示且不可点进树；无下属二级单位 B |
| 停用/仅目录不能从抽屉进入 | 领域切换仅 `FA-CNT-PROJECT,FA-I06,FA-CNT-OVERBUDGET,FA-CNT-WATCH-HIT,FA-I07,FA-I14,FA-OPEN`；无 FA-I01、无 EQ-* |
| 切换适用对象不同的指标 | 在 FA-P001 上切到重大资产低利用率净值占比：节点回到单位 A（不是总部），无 undefined；覆盖 **2/2** 与明细 2 个对象一致 |
| 只看异常 | 树只留当前指标高风险/关注对象（专用装备 A）；专用装备 B（0%/正常）从树隐藏；R02 标为「对象关联事项，不计入当前指标异常」 |
| 抽查其余五领域 | 股权/国际化/资金/产权/工程均可打开，切换均限本领域，无 undefined；覆盖 expected 与明细条数一致 |

截图：`overview_fa_i06_no_switcher.png`、`hq_drill_to_project_bg_unchanged.png`、`unit_a_scope_cannot_reach_b.png`、`switch_asset_indicator_no_undefined.png`、`only_abnormal_current_indicator_only.png`、`spot_check_equity_drawer.png`。

未完成：未开始固定资产等领域首页业务改造；未做 C01／C02；真实浏览器菜单 125% 缩放未验证。匿名 Vercel 临时部署已过期且 CLI 未登录，本轮未能覆盖更新公网部署；请用对话中的 Publish 发布。

## 资金与产权专项改版（FP-20260916-R2）

记录日期：2026-09-16。范围：资金管理、产权管理及系统配置目录。综合总览版式、标题、指标选用与交互保持；因业务办理与指标配置产生的统计联动允许变化（账户确认余额因专项专户样例变为 7152 / 受限 834.4 / 可用仍为 6317.6）。R07 1200/800/2000 与产权 60/60/55 未改写。新规则评估默认待核查，不计入未关闭整改，总览未关闭仍为 8。

实现提交 **`9ca874c`**；生产点击脚本 **`8113237`**；专题覆盖过滤见本提交（`configuredScenarios` 按 `topic_id` 收紧，避免 PTY-S01 等旧覆盖行混入产权交易专题）。`npm run verify` **169** 项通过。Chrome 生产构建 `scripts/verify-fp-r2.mjs` **39/39** 通过（对照 `9ca874c` 构建）。

| 验收项 | 实际点击结果 |
|---|---|
| FP-AC01 资金上区 | 标题「主体经营与财务状况」；盈利能力入口；主体路径；总部仅本级无独立报表；可下钻二级单位 |
| FP-AC01 六专题 | 账户管理、资金收付、融资与担保、资金运作、专项资金、经营风险 |
| FP-AC02 账户 | 账户专题打开账户档案 |
| FP-AC03 收付 | 命中对象筛选可见付款事实；正常付款仍在清单且可打开 |
| FP-AC05 法人全景 | 四卡 + 股权图 |
| FP-AC06 交易流程 | 经济行为切换；无偿划转无价款逾期；上市股份独立模板；横向肩形箭头 |
| FP-AC07 登记/控制 | 登记专题；控制专题 PTY2-S032 专业核查入口 |
| FP-AC08 配置目录 | 系统配置资金领域筛选含 CASH2 子场景 |
| FP-AC09 规则能力 | 规则表运行能力列；首批可试算 |
| FP-AC10 权限 | 配置管理员无业务旁路 |
| FP-AC11 专题切换 | 下区随专题更新；经营风险专题可进 |
| FP-AC12 总览保留 | 标题与固定资产卡仍在 |
| FP-AC13 正常与命中 | 均可打开依据 |
| FP-AC14 跨领域 | 产权事项可打开；价款收付同一 ID |
| FP-AC15 担保 | 打开被担保企业；股比不自动当控制 |
| FP-AC16 AI | 资金页不出现 FA-P001；任务绑定当前领域 |
| FP-AC17 分列 | CASH2-S033 SME-02 未评估；PTY2-S032 专业核查；正常付款 evaluated_clear |
| FP-AC18 布局 | 1440 / 1280 无整页横溢 |

截图：`fp_ac01_funds_profit_9ca874c.png`、`fp_ac02_account_drawer_9ca874c.png`、`fp_ac03_pay_hit_9ca874c.png`、`fp_ac05_equity_graph_9ca874c.png`、`fp_ac06_trade_flow_9ca874c.png`、`fp_ac08_settings_cash_9ca874c.png`、`fp_ac09_settings_rules_9ca874c.png`、`fp_ac12_overview_9ca874c.png`、`fp_ac14_cross_cash_9ca874c.png`、`fp_ac15_guarantee_9ca874c.png`、`fp_ac16_funds_ai_9ca874c.png`、`fp_ac18_overview_1280_9ca874c.png`。

未实现：其余约 70 条子场景仅维护定义、无执行器；无真实支付/融资/交易审批；财务报表为合成报告；CASH2-I10 未启用故不显示；CASH2-S901/S902 不计入 80。computerUse GUI 录像因模型额度不可用，点击验收由本机 Chrome/puppeteer 完成。

## 三项核清（容差 / 整改统计 / 账户报表差额）

记录日期：2026-09-16。不改资金、产权页面结构。`npm run verify` **215** 项通过。

1. **CASH2-S039 容差**  
   参数单位为万元，语义为货币精度。上限 0.01 万元（100 元）。输入 500 万元会被限制为 0.01，P-PAY001 实付 1200 / 批准 800、无有效批准变更，仍命中。历史评估 `FP-EVAL-S039-OK` / `FP-R2-1` 保留。配置发布验证改用 0.01，新评估追加不覆盖历史 ID。种子默认发布参数 `amount_tolerance_wan=0`。

2. **新旧整改统一统计**  
   未关闭整改 = 截至日快照中状态为整改中或待复核的事项明细，不按 R01—R08 锁死。种子默认截至日清单为 R02,R04,R05,R12,R13,R15,R17,R18（8 件）。R-FP-032 / R01 待核查不计入。PTY2-S032 于 2026-06-30 转整改中后总览与产权领域各 +1（9 件），由明细计算。办理日晚于 2026-06-20 时，该日仍还原为待核查，不改写历史。工作台、领域执行表按同一截至日快照。

3. **7152 与 6800 差额 352 万元**  
   监管账户：ACC-A +5080（与 FS-A 个别一致）、ACC-B +1480（与 FS-B 个别一致）、ACC-USD +72（境外无独立报表，待核实）、ACC-SPEC +520（受限不等于报表排除，待核实）、ACC-INT +0。合计 7152。总部合并 FS-HQ-CONS-2026H1 货币资金 6800。差额 352 标差异待核实。对照单位C个别 400 账户清单缺户，待核实。未编造 168 万元在途调节项。

## 中国大陆独立部署（本轮）

记录日期：2026-09-16。验收条件是中国大陆普通网络可访问、无需 VPN、关闭 Cursor/Agent/开发电脑后仍可用。

| 项 | 结果 |
| --- | --- |
| 国内云服务器 / 公司演示主机 / 已备案域名 | **本环境没有可用资源**（无云厂商凭证、无域名解析权限） |
| 固定 HTTPS 网址 | **无，未上云** |
| 部署包 | 仓库内 `Dockerfile`、`deploy/`、`npm run pack:cn`；说明见 `docs/中国大陆部署说明.md` |
| 应用形态 | Next.js `output: "standalone"` + nginx TLS；字体为系统栈；正式环境关闭 `/api/source-package` |
| 访客账号 | 不需要 Cursor / GitHub / Vercel；应用内角色与数据范围控制保留 |
| 大陆网络：综合总览 / 资金 / 产权 / 系统配置（直接打开、刷新、关键点击） | **待验证，不能宣称已通过** |
| Vercel / 临时隧道 | **不作本轮交付** |

本轮本地只验证部署包能否独立启动及资源是否自包含，出口网络不是中国大陆用户网络。

## 资金与产权增量（FP-20260916-R3）

记录日期：2026-09-16。范围：资金首页趋势与去单位表、同分类指标抽屉、六专题代表路径、产权先专题再交易流程、配置分类/趋势/有条件关联。综合总览版式保持。JSON 参考不覆盖 seed。

`npm run verify` **268** 项通过（含 R3 趋势/同分类/S040/S012/S029/S025/非法专题环节断言）。生产构建点击脚本 `scripts/verify-fp-r3.mjs` **24/24** 通过（对照 `a42ff23` 构建，1440/1280）。computerUse GUI 子代理因模型额度不可用，点击验收由本机 Chrome/puppeteer 完成。

| 编号 | 结果 | 说明 |
|---|---|---|
| R3-01 | 代码+口径通过 | 三类各 4 卡；首页无完整单位表；适用卡小趋势 |
| R3-02/03 | 代码通过 | 抽屉候选按 category_id 过滤 |
| R3-04/06 | 代码通过 | 无报表节点回到最近适用上级；树与总览同 ID |
| R3-07/08/09 | 口径通过 | 流量月发生额、余额期末、ROE 季度不年化 |
| R3-10/11 | 口径通过 | 缺期不补零；5 月 15 日无未来月末点 |
| R3-15/16/17 | 代码通过 | 80 条映射；S040/S012/S029/S031/S035/S039 及 PTY 代表路径启用 |
| R3-18 | 代码通过 | S011 主专题资金收付 |
| R3-21/22 | 代码通过 | 仅交易专题渲染经济行为与肩形流程 |
| R3-26/28 | 代码通过 | 标识 S025；R3.1 起产权四卡及详情固定不配趋势，资金趋势保持 |
| R3-31 | 代码通过 | 配置可改分类/趋势/专题；非法组合拒绝 |
| R3-32 | 沿用 | S039 容差 0；1200/800/2000；7152/352 待核实；60/60/55 |
| R3-34 | 待公网刷新后核对 | GitHub Pages `/-/`；大陆网络待用户实测 |

未完成：其余定义-only 子场景仍无执行器；无真实银企/产权审批；财务报表与月末快照为合成样例；CASH2-I10 未启用；截至 2026-05-15 无任意日财务快照（按设计隐藏趋势/标未覆盖）。

## 资金与产权下半区目录及产权指标显示（FP-20260916-R3.1）

记录日期：2026-09-17。范围：资金/产权下半区与场景配置，以及产权上半区指标显示（不常显公式、取消趋势图）。资金趋势、组织树、综合总览、首批启用规则、历史评估与整改保持。业需与对照表已入库：`docs/FP-20260916-R3.1/01_资金产权场景完整覆盖_R3.1.md`、`data/fp/primary_catalog_r31.json`。

一级目录来自对照表 11+10，不从命中或已启用子场景反向生成。产权交易仅在选定具体经济行为后显示肩形流程。

| 编号 | 结果 | 说明 |
|---|---|---|
| R31-01 | 待实点 | 资金默认全部专题，11 项一级可见可展开 |
| R31-02 | 待实点 | 产权默认全部专题，10 项一级可见；无经济行为及流程 |
| R31-03 | 待实点 | 仅交易有下拉；选定经济行为后才有肩形流程；回全部恢复 10 项 |
| R31-04 | 待实点 | 登记/标识与名称资质/股权与控制权无流程、无独立导航大卡 |
| R31-05 | 待实点 | 资金专题映射正确，回全部恢复 11 项 |
| R31-06 | 待实点 | 未启用/未具备运行条件/未开展监测分列，不伪 0 命中 |
| R31-07 | 待实点 | 泛称不占 11/10；标准子场景不标补充 |
| R31-08 | 待实点 | 统计联动与历史办理保持 |
| R31-09 | 待实点 | 1440/1280 同卡可读 |
| R31-10 | 待实点 | 输出 11/11、10/10；Pages 实现提交与部署提交分开 |
| R31-11 | 待实点 | 产权四卡及详情无公式常显、无趋势 |
| R31-12 | 待实点 | 资金适用趋势仍在 |

未完成：其余定义-only 子场景仍无执行器；大陆网络待用户实测。




# 验收记录（V1.6）

记录日期：2026-09-15。原型功能完成状态与真实系统条件核实状态分开。未连接生产接口、未做正式登录与模型推理。

## 已验证（计算与口径，`npm run verify`）

对照 `demo_seed.json` 的 `expected_results` / `expected_stage_results_v1_2`：

- 总部固定资产投资计划执行率 84%（8400÷10000）
- FA-P001 80%、FA-P003 90%
- FA-P001 预计完工投资偏差 18%
- 建设实施 FA-S20 五数 2/1/1/0/0；运营及后评价 FA-S29 五数 2/1/1/0/1
- 恢复种子未关闭 8、高风险 4、逾期整改 1；R09 已排除
- 账户 6632 / 受限 314.4 / 可用 6317.6

覆盖规划候选（358）不进应评估分母；已完成观察（31）进入五数。

## 本轮增量已落地（页面与交互）

| 项 | 结果 | 说明 |
|---|---|---|
| 侧栏 248px、系统配置入口、规范文案 | 通过（代码） | 业务标题/按钮不显示 P71–P79；不出现「重置演示数据」 |
| 综合总览 84/8/4/1 | 通过（代码+口径） | 指标打开组织穿透 |
| 计算依据按指标绑定 | 通过（代码） | 执行率与 EAC 偏差各自公式 |
| 六领域首页 KPI 4–6 张、资金专题、国际化事件 | 沿用并授权过滤 | 未删领域 |
| AI 预置分析 | 通过（代码） | 未连接模型；越权任务提示超范围 |
| R07 合成材料显式采用、独立复核 | 通过（代码） | 草稿不自动当证据；自审禁用 |
| 配置六 Tab、试算/发布、双重置 | 通过（代码） | 配置与业务分键保存 |
| 单位 A/B 隔离、配置管理员无业务数据 | 通过（代码） | 查询链求交授权组织/对象 |
| 实施调研对应表 | 已提供文档 | 来源系统待科技信息部核实 |

## 未宣称 / 待调研

- 真实组织目录、单点登录、服务端鉴权
- 投资计划/完成、付款批准与实付的真实接口字段与更新频率
- 外部行情订阅与模型服务
- 全公司覆盖规模或上线成效

## 第二批（范围、期间、覆盖、权限）

对应检查报告 R04—R07、R14。在 `4ab8161` 第一批 UI 成果上修复已有机制的执行遗漏，不新增审批层级或基层填报。

| 路径 | 结果 |
|---|---|
| 总部含下级打开执行率抽屉 | 背景与抽屉初始值均为 84% |
| 总部仅本级打开执行率抽屉 | 背景与抽屉均为无业务，不再自动变成含下级 84% |
| 单位 B | 执行率 90%；未关闭事项显示 0；Tab/AI/导出/试算不出现单位 A 事实 |
| 总部管理人员A（只读） | 不能采用材料、督办、发布规则或重置 |
| 配置管理员A | 可看配置定义；试算仅独立测试输入，不读总部业务金额 |
| 切换期间 | 快照指标显示「该期间数据未覆盖」；资金/工程收付款按交易日期过滤；H1 仍为 84/80/90 |
| FA-S02、FA-S21 | 未确认候选不进入业务应评估分母 |
| R07 整改与独立复核 | 有权人员仍可完成；跨领域同步；1200/800/2000 不变 |

`npm run verify`：原口径 48 项 + 第二批 24 项，共 72 项通过。

## 环境限制

远程环境内部 `localhost` 不能当作外部用户入口。本轮沿用已授权预览转发；不为获得链接新建外部隧道。

UI 缩放：CDP 页面缩放模拟已检查，真实浏览器菜单 125% 仍待验证。

## 综合总览排版与事项清单浮层（本轮）

范围：只改综合总览 KPI / 领域卡 / 表格排版，以及事项数字点击后的清单浮层。未改股权默认配置、矩阵统计口径、事件期间筛选、规则执行绑定。

| 项 | 结果 |
|---|---|
| 顶部 KPI 标题、主数字、辅助信息分槽对齐 | 已改 `KpiCard`；金额拆成「完成 n 万元」「同期计划 n 万元」两段，万元不拆行 |
| 1440 四列 / 1280 两列 | 沿用 `.reg-kpis` 既有断点 |
| 六领域卡标题、指标、「进入领域」网格对齐 | 股权无首页指标时显示空态，不写 62.5% |
| 单位风险分布列宽与表头「合计（去重）」 | 表内横滚，不改 0 / 无业务 / 未评估 口径 |
| 1280 待复核与近期事件上下排列；宽屏顶对齐、空表不拉高 | `.reg-split` 在 1440 起两列 `align-items: start` |
| 事项数字打开清单 Modal，不再追加页底 | 关闭详情回到清单；关闭清单恢复滚动位置 |

### 后续功能纠错（本轮未执行）

- 股权 EQ-I11 默认停用/仅目录，总览股权卡无 62.5% 主指标
- 事项清单仍按点击时 ID 展示；切换组织或期间后清单可能陈旧
- 风险矩阵仍用整单位 `hasBusiness`，「本次监测未发现异常」与未评估未分开
- 近期事件未按所选期间过滤，点击不定位到国际化对应事件
- C01 / C02 规则执行绑定与发布版本
- 真实浏览器 125% 缩放未在本环境验证

## 全站 UI 统一（本轮）

以综合总览已确认样式为基准，优先改共用组件后铺到各业务页、工作台、配置 Tab 与浮层。未改指标公式、金额、权限、场景映射和办理逻辑。

| 项 | 处理 |
|---|---|
| KPI 分槽对齐、单位不拆行、百分号一次 | 沿用 `KpiCard`；领域首页改用 `.reg-kpis-domain` |
| 筛选区紧凑换行 | `FilterBar` 收窄宽度，`PageHeader` 与筛选同行可折行 |
| 流程条紧随指标下方 | `DomainPage` 先流程后重点关注，便于 1440 首屏看到核心指标和肩形箭头 |
| 流程名称完整 | `ChevronFlow` 按名称加宽，容器内横滚 |
| 长表分页、名称列宽度、金额右对齐 | `DataTable` 增加 `pageSize`/`minWidth`；分页只切展示行 |
| 配置双列表 1280 上下排、空表不拉高 | `.reg-split` |
| 浮层层级与 AI 入口 | 沿用现有 overlay 栈与顶栏/标题栏 AI 入口 |

已知业务逻辑问题仍见上一节「后续功能纠错」，本轮未处理。

## 全站 UI 收尾（本轮）

基于 `8eb4ca5`，只处理配置操作区、页面滚动策略与遗漏独立页样式。未改股权默认配置、清单 ID、矩阵口径、事件筛选、C01／C02。

| 项 | 处理 |
|---|---|
| 配置操作列 | `DataTable` 支持 `sticky: "right"`；查看／编辑／启用停用固定在表右，不随内容横滚离开视口 |
| 配置双列表 | 场景 Tab 改为 `.reg-settings-pair` 始终上下排，1440 半栏不再裁切停用按钮 |
| 页面滚动 | 未缩小 KPI／按钮／流程条；1280 固定资产 7 张指标下移流程条仍可滚动 |
| 对象档案独立页 | `/object/[id]` 不再用抽屉盖住侧栏；沿用 `PageHeader`、表格分页与全站表样式 |
| 场景规则库独立页 | 顶栏恢复入口；页内改用 `PageHeader`、长表分页与名称列宽度 |
| 真实业务状态 | 未发布／未执行等状态照常展示 |

### 后续功能纠错（本轮仍未执行）

- 股权 EQ-I11 默认停用/仅目录，总览股权卡无 62.5% 主指标
- 事项清单仍按点击时 ID 展示；切换组织或期间后清单可能陈旧
- 风险矩阵仍用整单位 `hasBusiness`，「本次监测未发现异常」与未评估未分开
- 近期事件未按所选期间过滤，点击不定位到国际化对应事件
- C01 / C02 规则执行绑定与发布版本
- 真实浏览器 125% 缩放未在本环境验证

## 三项展示收尾（本轮）

保留 `cae0637` 及此前已统一的侧栏、配色、卡片、表格、流程和浮层。只改正单位转换、状态重复拼接与场景开发文案。未改指标配置、取数口径、权限、流程、办理或发布逻辑。

| 项 | 处理 |
|---|---|
| 资金底部合计 | 账户原币×汇率为元，展示前折万元，与 KPI 同口径按当前筛选汇总 |
| 超概关注规则命中 | `formatKpiCaption` 使「关注规则未发布，未执行」只出现一次；主数值仍为 — |
| 场景文案 | 「补充监管场景」「监管指标」；列表改为「查看依据」；平台行为/排除操作写入实施说明 10.4 |

股权指标、清单陈旧、矩阵状态、事件筛选与跳转、C01／C02 仍未执行。

## 综合总览监管主体全景（本轮）

范围：只改综合总览结构、首页统计口径与必要联动；不改各领域首页业务布局，不启动固定资产等领域改造，不处理 C01／C02、真实接口或 AI 接入。

业需与计算依据写入：

- `docs/系统原型定位与实施调研说明_V1.6.md` 第 2 节
- `docs/海油工程穿透式监管平台_完整业需_V1.6.md` 第 6 章、第 16.2 节、第 23.2 节及 AC101
- 实现口径：`lib/overview.ts`

| 项 | 处理 |
|---|---|
| 两主区块 | 监管主体全景 + 专项领域监管概况；去掉摘要、待复核表、外部事件表、单位×领域矩阵、首页重点事项表 |
| 四项总体数字 | 纳管单位（组织 ID 去重，总部在范围内计入）、在管项目（FA+EQ+ENG 去重）、高风险责任单位（实际责任单位 ID）、未关闭高风险（附逾期高风险） |
| 主体层级图 | 根为海油工程总部；默认总部+直属二级。选中后只展示该单位及其直属下级，上级留在路径；点击选中并同步筛选 |
| 领域卡 | 每领域两项主指标 + 短异常标签 + 进入领域；FA 计划执行率回到固定资产卡 |
| 启停 | EQ-I11 / EQ-I08 保持未启用，对应空态；不批量改目录 |
| 浮层 | 筛选或身份变化关闭旧清单/抽屉；抽屉钻取不改背景组织 |

固定资产领域业务改造、矩阵 0/无业务/未评估纠错、事件期间筛选、C01／C02 本轮仍未执行。

### 本轮交互核对（非路由 200）

提交 `797db3b`。`npm run verify` 85 项通过。Chrome 实机（1440/1280，清本地状态后）：

| 路径 | 结果 |
|---|---|
| 总部默认 | 纳管单位 6（含总部）、在管项目 6、高风险责任单位 2、未关闭高风险 4／逾期 1；两主区块名称正确；无矩阵/待复核/事件表；股权偏差「指标未启用」 |
| 点二级单位 A 并展开三级 | 筛选变为下属二级单位 A（含下级）；单位 3／项目 4；路径与返回总部可用；A1、境外分支 A2 出现；B/C 无虚构三级 |
| 进入固定资产再回总览 | 领域页与返回后均保留下属二级单位 A（含下级）及原期间 |
| 高风险数字 → 清单 → 详情 | 清单 4 条与数字一致；详情叠在清单上 |
| 指标抽屉钻取 A1 | 背景筛选仍为单位 A；抽屉内 A1 执行率 80% |
| 切换期间、返回总部、切配置管理员 | 旧事项清单关闭 |
| 1440 / 1280 横溢 | `scrollWidth===clientWidth`，无整页横向溢出 |

未验证：真实浏览器菜单 125% 缩放；未开始固定资产等领域业务改造。

## 综合总览口径改版与扩容（本轮）

提交 `36e8660`（实现主体 `912f76e`，本提交补整改卡标题可点与 Chrome 交互核对脚本）。`npm run verify` 口径脚本 93 项通过，**不以该项替代页面点击**。

Chrome（`scripts/verify-overview-ui.mjs`，1440/1280，清 `localStorage` 后，生产服务 `http://127.0.0.1:43917`）**33 项交互全部通过**。

总部默认可见数字（2026 年上半年 / 截至 2026-06-30 / 总部含下级）：

| 指标 | 页面数字 | 对应清单 |
|---|---|---|
| 纳管单位数 | 22 家 | 清单 22 家；不含总部财务部、单位A项目部 |
| 纳管项目数 | 81 个 | 清单 81 条，可按固定资产/股权/工程筛选 |
| 规则命中涉及单位数 | 7 家 | 清单 7 家，均为对象实际归属单位 |
| 未关闭整改事项数 | 8 件 | 清单 8 条；本期完成 1、逾期 2 |

| 验收项 | 结果 | 说明 |
|---|---|---|
| 停用 FA-I06 后首页隐藏，再启用恢复 | 通过（点击） | 固定资产投资卡不再出现投资完成额、投资计划执行率；启用后两项均恢复 |
| 总部 → 单位 A → A1 → 关联对象 | 通过（点击） | 筛选变为下属二级单位A（含下级）；A1/A2 并排；末级打开基地能力提升项目档案 |
| 顶部数字与清单条数 | 通过 | 22/81/7/8 与浮层「共 n 家/条」一致 |
| 命中单位不重复祖先 | 通过 | 7 家为 A1、D、D海工、E、E安装、F、A2，无总部、无单位 A 汇总节点 |
| 命中规则不按运行次数 | 通过 | 监测浮层写明「不按运行次数累加」；对象按类型分列 |
| 待核查不计入未关闭整改 | 通过 | 8 条均为整改中或待复核，无待核查、无已排除 |
| 规则停用后历史仍可查 | 通过 | 停用 FA-R20-01 后未关闭整改仍 8 件；命中层仍见 FA-R20-01 |
| 切换单位 A 身份 / 配置管理员 | 通过 | 单位 A 看不到下属二级单位 D；配置管理员见无业务数据权限 |
| 1440 / 1280 | 通过 | `scrollWidth===clientWidth`；所属单位下属区 `max-height:420px` 滚动，不铺满首页 |
| 无介绍摘要、名词解释、演示字样、未启用指标占位 | 通过 | 股权「现金回报目标偏差」未出现；无「指标未启用」 |

截图：`overview_hq_1440_36e8660.png`、`overview_unit_a_1440_36e8660.png`、`overview_monitoring_list_1440_36e8660.png`、`overview_rectification_detail_1440_36e8660.png`、`overview_hq_1280_36e8660.png`。

### 本轮未完成 / 未在浏览器再点一遍

- 未开始固定资产投资等领域首页业务改造，未做 C01／C02。
- 截至日筛选仍固定 2026-06-30；`statusAtAsOf` 已按当时状态还原，但界面没有第二个历史截至日可点。
- 本轮浏览器未再现场办理「复核关闭」一条新事项；本期完成 1 件来自种子已关闭事项（R11），口径脚本覆盖重开不计入完成数。
- 单位清单「纳管项目」列按该单位本级对象计，单位卡按该节点监管范围计；点击单位卡项目数字已验证只开清单、不切换组织，未逐张对每张卡的数字与清单条数。
- 真实浏览器菜单 125% 缩放仍未验证。
- 事项详情抽屉内仍有既有「Demo 口径」提示，业务总览首页未新增「演示」字样。


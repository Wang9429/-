# 验收记录（V1.6）

记录日期：2026-09-15。原型功能完成状态与真实系统条件核实状态分开。未连接生产接口、未做正式登录与模型推理。

## 已验证（计算与口径，`npm run verify`）

对照 `demo_seed.json` 的 `expected_results` / `expected_stage_results_v1_2`：

- 总部固定资产投资计划执行率 84%（8400÷10000）
- FA-P001 80%、FA-P003 90%
- FA-P001 预计完工投资偏差 18%
- 建设实施 FA-S20 五数 2/1/1/0/0；运营及后评价 FA-S29 五数 2/1/1/0/1
- 恢复种子未关闭 8、高风险 4、逾期整改 1；R09 已排除
- 账户 6632 / 受限 314.4 / 可用 6317.6

覆盖规划候选（358）不进应评估分母；已完成观察（31）进入五数。

## 本轮增量已落地（页面与交互）

| 项 | 结果 | 说明 |
|---|---|---|
| 侧栏 248px、系统配置入口、规范文案 | 通过（代码） | 业务标题/按钮不显示 P71–P79；不出现「重置演示数据」 |
| 综合总览 84/8/4/1 | 通过（代码+口径） | 指标打开组织穿透 |
| 计算依据按指标绑定 | 通过（代码） | 执行率与 EAC 偏差各自公式 |
| 六领域首页 KPI 4–6 张、资金专题、国际化事件 | 沿用并授权过滤 | 未删领域 |
| AI 预置分析 | 通过（代码） | 未连接模型；越权任务提示超范围 |
| R07 合成材料显式采用、独立复核 | 通过（代码） | 草稿不自动当证据；自审禁用 |
| 配置六 Tab、试算/发布、双重置 | 通过（代码） | 配置与业务分键保存 |
| 单位 A/B 隔离、配置管理员无业务数据 | 通过（代码） | 查询链求交授权组织/对象 |
| 实施调研对应表 | 已提供文档 | 来源系统待科技信息部核实 |

## 未宣称 / 待调研

- 真实组织目录、单点登录、服务端鉴权
- 投资计划/完成、付款批准与实付的真实接口字段与更新频率
- 外部行情订阅与模型服务
- 全公司覆盖规模或上线成效

## 第二批（范围、期间、覆盖、权限）

对应检查报告 R04—R07、R14。在 `4ab8161` 第一批 UI 成果上修复已有机制的执行遗漏，不新增审批层级或基层填报。

| 路径 | 结果 |
|---|---|
| 总部含下级打开执行率抽屉 | 背景与抽屉初始值均为 84% |
| 总部仅本级打开执行率抽屉 | 背景与抽屉均为无业务，不再自动变成含下级 84% |
| 单位 B | 执行率 90%；未关闭事项显示 0；Tab/AI/导出/试算不出现单位 A 事实 |
| 总部管理人员A（只读） | 不能采用材料、督办、发布规则或重置 |
| 配置管理员A | 可看配置定义；试算仅独立测试输入，不读总部业务金额 |
| 切换期间 | 快照指标显示「该期间数据未覆盖」；资金/工程收付款按交易日期过滤；H1 仍为 84/80/90 |
| FA-S02、FA-S21 | 未确认候选不进入业务应评估分母 |
| R07 整改与独立复核 | 有权人员仍可完成；跨领域同步；1200/800/2000 不变 |

`npm run verify`：原口径 48 项 + 第二批 24 项，共 72 项通过。

## 环境限制

远程环境内部 `localhost` 不能当作外部用户入口。本轮沿用已授权预览转发；不为获得链接新建外部隧道。

UI 缩放：CDP 页面缩放模拟已检查，真实浏览器菜单 125% 仍待验证。


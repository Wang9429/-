# 海油工程穿透式监管平台：Cursor与Codex开发任务说明

版本：V1.6｜2026年9月15日｜用途：在Cursor已开发的现有Demo上增量改版；亦适用于Codex接续开发

本业需包同时用于Cursor和Codex，业务需求、页面交互、场景目录及示例数据共用同一套Markdown/JSON文件。开发工具由使用者选择；实际完成情况必须由运行、计算及交互验收确认。

用户已在Cursor中开发并试用Demo。本文件交付的是本轮改版任务及验收要求，不代表现有代码已完成本轮修改或通过验收。沿用现有工程、技术栈、依赖、稳定ID及业务数据。业务页面使用规范名称和匿名单位名称，在统一“数据说明”中明确主体、人员、金额、交易、汇率、行情和分析为合成样例，不代表海油工程真实经营数据；真实历史事件日期与样例行情分别说明。

## 1 本轮怎么交给Cursor或Codex

两种工具共用本包，**本轮从Cursor已开发的现有工程继续修改**。把V1.6文件放入现有工程的需求目录；保留旧版供追溯，明确V1.6为当前版本，不创建第二套网站。

1. 首次接续V1.6时，先读当前工程适用的`AGENTS.md`、本文件第8节及《系统原型定位与实施调研说明_V1.6.md》第1至8节及第10至11节，定位现有综合总览、共用布局、指标抽屉、事项和权限实现。操作示例按需读第9节，实施调研与交付范围读第10至11节。之后只读与当前批次有关的主业需章节和数据记录；已有`AGENTS.md`合并保留工程约定。
2. 先确认现有页面、项目启动方式与数据存储位置，再按第5节分批修改。已有依赖满足需求时直接复用，依赖没有改变时不重装；不因这次业需更新而重建工程、重写全部页面或迁移框架。
3. 后续每批只读受影响章节和代码。投资细化按当前领域、子场景ID、指标ID检索适配对照和catalog；配置数据只取相关结构和记录，不重复载入Excel原文或整份巨大JSON。用户反馈具体问题时按范围修复，并检查受影响公共能力。
4. 每批完成后检查相关交互、数值和页面截图，保留工程明确要求的检查；没有具体剩余风险时，不反复执行全量测试、构建或录屏。最终汇总实际验收记录和未完成项。

Cursor和Codex都可使用第8节指令。可用模型、按钮位置和运行能力以实际环境为准；本交接不要求切换工具、购买服务或安装特定插件。

### 本轮改版摘要

| 顺序 | 必须落地的改动 | 预期成果 |
|---|---|---|
| 第一批 | 综合总览、共用视觉与业务文案、固定资产原型路径及功能到数据的映射 | 首页清楚表达业务执行及重点事项；侧栏可读；84%组织穿透、18%成本依据与阶段联动可用 |
| 第二批 | 六领域差异化、跨域关联、AI、核查与整改材料 | 代表性故事完整；同一事项多领域同步，分析有依据，办理与独立复核有材料 |
| 第三批 | 系统配置、角色与维护职责确认、实施调研资料和整体核对 | 六页配置可用，规则试算与发布区分，权限与保存生效；功能可关联数据和调研问题，提供可访问预览 |
| 每批收口 | 保存与恢复、受影响交互验证、真实预览入口 | 已有业务基准可复算，新增配置可保存并生效，用户能实际打开查看 |

## 2 输入文件与适用顺序

| 文件 | 用途 |
|---|---|
| 系统原型定位与实施调研说明_V1.6.md | 本轮入口：项目原型定位、页面与业务确认、科技信息部调研映射、操作示例及合成材料 |
| 海油工程穿透式监管平台_完整业需_V1.6.md | 产品行为、页面、监管链路、统一计算、配置与权限、AI及AC01至AC111原型/资料核对要求；第24章新增科技信息部调研映射 |
| 公开产品借鉴与页面设计_V1.6.md | 公开产品借鉴、页面结构、蓝色视觉、侧栏尺寸及规范文案 |
| 投资底稿适配对照_V1.4.md | 投资底稿53个子场景逐项适配、保留的原KRI、规则落地方式及人工/AI边界 |
| investment_catalog.json | 53项投资子场景、38项原KRI、稳定ID、原文审计元数据及本版执行方式和阶段归属 |
| demo_seed.json | 统一组织、业务对象、事实明细、关系、风险、时间及验收基准；业务金额和稳定ID继续保留 |
| configuration_seed.json | 用户、角色、数据范围、配置导航、场景/规则/指标/AI与数据配置的初始状态和示例 |
| 开发任务说明_Cursor与Codex.md | 本轮实施顺序、增量开发要求、可复制指令与交付要求 |
| AGENTS.md | 在代码迭代中持续遵守的项目约束 |

上述九件文件供完整追溯和按需查询，不要求首次或每轮全文读取。初次先建立必要的工程架构、权限及本轮范围理解；处理投资时按领域/子场景ID/指标ID查适配和catalog，处理配置时只读相应配置结构，JSON原文审计数据仅在需核对来源时读取。产品行为以完整业需为准，公开产品参考服务于本平台的监管任务；投资逐项适配以V1.4适配对照为准，JSON负责稳定ID及初始配置。原53项子场景和38项KRI保持可追溯，不因首次可计算的指标较少就删掉完整目录。

旧版“40项结构化监测、10项线索核查、3项专业核查依据”仅作为历史适配标签保留，**不是40项均可自动运行或已接入数据的结论**。实际执行方式按本版目录和规则配置确定；规则实例的适用性、数据准备情况、执行结论分别记录。

不能把`original_columns`中的源系统审批、支付拦截、资产处置或处罚动作改造成本平台的操作，也不能将原始Excel列号直接显示给用户。旧版与V1.6冲突处以本轮用户已明确的意见及V1.6展示要求为准，V1.4投资取舍与数据契约继续保留；未知数据不得补成正常。

## 3 产品必须保持的主线

一级导航顺序固定为：**综合总览、固定资产投资管理、股权投资管理、国际化业务、资金管理、产权管理、工程项目管理**。导航次序改变不要求重编既有页面编号、对象ID或路由。

侧栏底部增加独立的“系统配置”入口，内部页面标识P76，路由`/settings`；它与上方七个业务入口分区排列，不作为第七个监管领域。顶部“监管工作台”入口汇聚当前用户待核查、待整改、待复核及督办事项。共用数据追溯和业务关联从指标、对象或事项上下文进入。P71—P79只用于研发定位，页面标题、按钮、提示和导出内容不得显示这些编号。

平台是从总部及所属单位开展监管的工作台。用户应能从组织及指标找到对象，沿业务链查看上下游关系，解释规则为何命中，打开对应材料，再开展本平台的核查、整改和复核。源系统的投资审批、招采、签约、付款、记账、正式产权办理和责任追究只展示事实、结果及来源。

需要同时实现三种进入路径：

| 进入路径 | 用户要获得的结果 |
|---|---|
| 组织及指标穿透 | 从海油工程总部到二级、三级单位及项目/资产/账户等末端对象，查看构成和业务明细 |
| 业务链及对象联动 | 从同一投资项目、资产、被投企业、合同、付款或产权事项，查看关联领域中的同一对象及依据 |
| 场景及规则追溯 | 从阶段或专题进入场景，查看适用条件、口径、阈值、版本、命中事实及材料位置，进入核查整改 |

组织管理树与法人/产权关系分开。二级直管项目直接挂接，不补造三级；被投企业不因有股权关系自动进入管理组织树。对象缺少关系时显示缺口，不构造虚假链路。

### 3.1 首页直接完成阶段选择

领域首页顺序为“标题及全局筛选—领域重点指标—横向肩形箭头/专题—场景执行清单及对象事项”。固定资产、股权、产权、工程四领域都在监管总览首页中间直接显示可点击的横向肩形箭头。每个箭头只放**阶段名称＋未关闭事项数**，名称完整可读，不放对象数、场景数、数据覆盖、进度百分比或状态堆叠。

点击任一箭头，直接更新本页下方的场景执行清单及其对象、事项。清单提供监测对象、命中对象、未关闭、本期已整改闭环、逾期整改数五个数量入口。没有先切换“流程监管”Tab或进入矩阵的前置步骤。详细核查表、项目时间线或阶段资料作为选中阶段下的可选展开内容，不替代首页箭头和清单。

全局组织、含下级/仅本级、期间及截至日改变时全页按同一口径重算；阶段及阶段子视图仅筛选下方区域。顶部KPI明确标注“当前组织及期间·领域范围”，不随阶段点击偷偷变成阶段KPI。下区显示当前阶段标签，可清除恢复该领域全部阶段；切换全局范围后仍适用的阶段可保留并重算，无对象时显示具体提示。

固定资产八阶段依次为：**储备谋划、可研论证、决策审批、设计概算、采办管理、建设实施、验收投用、运营及后评价**。“运营及后评价”下区提供“资产运营/投资后评价”子切换，默认资产运营；投用后资产持续运营，后评价按适用条件及评价时点开展，不能实现为先完成后评价才能运营。股权、产权、工程节点名称及条件性、并行关系按完整业需V1.6及对应模板，不自行编造业务硬流程。资金继续使用专题，国际化继续使用地区/事件和影响对象。

箭头顺序表达业务监管视角，不等同所有项目必须串行经过每一节点。工程允许设计、采购、建造等并行；条件性节点按对象适用性显示，说明位于条下或选中阶段的详细区域。已完成或历史阶段只要仍有关联未关闭事项，节点和下方清单就保留这些问题，不仅取对象当前阶段。

### 3.2 指标点击打开P71大抽屉或弹窗

点击首页指标卡默认打开P71大抽屉或大弹窗，保留背景首页及其状态。浮层顶部显示指标、单位、期间、截至日、当前范围及关闭入口；左侧为“海油工程总部—二级单位—三级单位—项目”的可展开树，右侧显示选中节点的指标值、目标/偏差、分子分母、构成和对应业务明细。账户、资产、被投企业等按指标实际末端对象接续；缺层按真实关系跳过。

选择树节点只改变浮层中的指标范围，不修改背景页全局组织。右侧明细可继续查看数据追溯P78、业务关联P79及证据，保留返回P71的节点和展开状态。关闭浮层或完成返回后，背景页面的组织、期间、阶段/子视图、清单筛选、页码、滚动位置保持原样。保留P71页面编号；允许直接链接作为补充入口，但不以整页跳转替代指标卡的默认浮层体验。

### 3.3 五个数量的统计边界

观察归属统一按业务窗口结束日`window_end`落入所选期间（含端点），且评估取得时间不晚于截至日（按主文的业务取得/生效时间口径，不能用实际操作日志`recorded_at`替代）；季度或滚动窗口不按重叠月份拆分分摊。监测记录按实际评估关联的阶段进入清单，不因catalog把场景关联到多个阶段就自动铺满监测对象。有效历史命中即使最新评估正常仍保留；只有确认误报等有效排除才移出命中。人工核查有明确线索可以计命中，无结论显示“—”。整改截止日缺失显示“缺期限”，不当作未逾期。

| 数量 | 统计与点击要求 |
|---|---|
| 监测对象 | 当前范围内已实际评估至少一条适用规则或已完成人工核查的唯一对象；按对象类型＋对象ID去重。应评估与已评估分开，部分规则完成仍显示部分覆盖；点击查看监测记录与缺口 |
| 命中对象 | 当前观察期自动规则命中或人工评估发现明确线索的唯一对象；含已整改闭环的命中历史，确认误报并排除的不计入；同一对象多次命中去重，按对象类型显示并可查看依据 |
| 未关闭 | 当前范围截至日状态未关闭且未排除的唯一risk_id；进入同一事项清单 |
| 本期已整改闭环 | 当前范围内所选期间复核通过关闭且截至日仍为closed、close_reason=rectified的唯一risk_id；整改提交、待复核、已排除均不计入 |
| 逾期整改数 | 当前范围内已进入整改责任范围、截至日仍未关闭（含待复核）且超过有效整改截止日的唯一risk_id；待复核不自动停表；核查或复核其他办理SLA超期不混入 |

资产运营子视图默认按资产数，投资后评价子视图按项目数；末阶段箭头将两者的未关闭事项按risk_id合并去重。未关闭/逾期整改数是截至日存量，本期已整改闭环是期间内完成且截至仍关闭（close_reason=rectified）的数量，不用“本期已整改闭环÷（未关闭＋本期已整改闭环）”计算闭环率。

核查、复核等其他办理SLA超期在监管工作台单列，不计入上述第五数。

场景行、阶段汇总和领域汇总各自按所属范围去重，不能直接相加场景行或阶段行形成上级数。项目、资产、付款等不同类型不混算成一个模糊“对象总数”；按主文明确的类型或分项展示。历史阶段问题和跨阶段关联按规则/事项关联纳入，不因项目已进入下一阶段就消失。

### 3.4 全平台采用统一蓝色主题

本Demo面向国央企总部监管人员，视觉定位为稳重、精致、清晰的企业管理系统。高端感通过统一排版、适度留白、清楚的层级、整齐数字和细致交互实现。综合总览、六业务领域、监管工作台及共用详情全部使用同一套主题和组件。

| 元素 | 设计要求 |
|---|---|
| 一级侧栏 | 深蓝`#0B1F3A`；默认文字`#E7EFFA`、15px，选中白色。展开宽度248px（可在240—264px内统一调整），`flex: 0 0 248px`，不被主区挤压；固定资产在股权上方，底部为系统配置 |
| 主色及交互 | 主蓝`#1D5FD1`用于主要按钮、链接、选中指示及重点业务数据；交互悬停、键盘焦点和禁用状态分别设置 |
| 页面与卡片 | 页面浅灰蓝`#F3F6FB`，卡片白色`#FFFFFF`，细边框和轻阴影；保持统一圆角、卡片间距和内容对齐 |
| 文字与数值 | 深色正文清晰可读；金额和百分比位数一致，金额列右对齐，单位及期间明确；不用装饰性字体或小号浅灰文字承载关键内容 |
| 指标与流程 | KPI突出值、单位和必要比较；横向肩形箭头继续保持阶段名＋未关闭数，选中状态用蓝色边框或描边，监管风险状态仍使用独立红黄语义 |
| 图表与详情 | 常规比较采用有限层次的蓝色系列；风险红、黄、正常绿、未评估灰分别配文字/图标。指标浮层、组织树、场景表、规则、证据和整改窗口沿用统一视觉 |

蓝色表达品牌及交互，不表示风险正常。不得把高风险改成蓝色、仅靠色彩区分状态，或把选中阶段的蓝色误当成风险等级。首页以可用的监管工作台为核心，不添加无实际监管用途的旋转地球、发光网格、霓虹描边、满屏渐变或装饰动画。不得编造官方标识或授权背书。

在1440×900优先验证完整业务入口、重点指标、流程/专题和场景清单的衔接。四流程领域首屏能看到横向箭头；阶段名称清晰完整，空间不足使用受控横向滚动，不压成难读的小字或复杂折线。P71和其他浮层的标题、关闭、树和明细均可操作。保留AC01至AC70既有要求，补充AC71至AC100的侧栏、配置、权限、文案和AI验收；各批按改动影响选择检查，不要求每次重跑全量。

### 3.5 侧栏和页面文案必须在共用组件修复

布局采用侧栏固定宽度、主内容`flex: 1; min-width: 0`。侧栏内部图标固定宽度且不缩小，图标与文字间隔8—12px；文字容器设`min-width: 0`，正常展开状态保证七个标准菜单名称及“系统配置”完整可读。个别超长自定义名称可在菜单内部换行或省略并提供完整名称提示，不得让文字溢出菜单、侵占主区或通过极小字号解决。侧栏的上部菜单可独立滚动，底部配置入口始终可达；顶部品牌也需在侧栏宽度内排版。

业务字段统一使用“一级监管场景、监管子场景、场景描述、管控要求、监控指标、监测规则、执行方式、规则依据、计算依据、来源记录”等语义名称。原文件名、行列、`original_columns`及研发字段仅存审计元数据，不能自动生成业务字段标签。指标和对象稳定ID可在技术定位或必要编号位置保留，但页面标题、按钮不得拼入P71、P78等内部页面编号。

页面统一标题为“海油工程｜穿透式监管平台”；业务主体按“下属二级单位A/B/C”“下属三级单位A1”“境外分支机构A2”等匿名命名，保留原组织和对象ID。业务界面不出现“演示二级单位”“演示项目”或“演示数据重置”；必要入口改为“数据说明”“重置业务办理状态”“重置配置”等规范名称。真实数据属性在集中数据说明中写明“本系统当前采用合成样例，用于功能验证，不代表海油工程真实经营数据”；AI面板另明确其真实连接状态。不能通过去掉“演示”二字冒充真实海工事实。

### 3.6 系统配置必须有可操作页面

系统配置固定放侧栏底部，进入`/settings`后有六个Tab：

| Tab标识 | 页面名称 | 最低可操作内容 |
|---|---|---|
| users | 用户与权限 | 用户、角色、菜单和操作权限、授权组织范围、本级或含下级、数据范围预览及保存 |
| scenarios | 监管场景 | 一级监管场景与监管子场景维护，新增/编辑/停用，归属领域、阶段、适用对象、责任部门和版本 |
| rules | 监测规则 | 规则条件、数据前提、执行方式、适用范围、阈值/边界、结果说明、试算、发布及历史版本 |
| indicators | 指标管理 | 指标名称、单位、计算公式、输入字段、汇总方式、频率和口径、关联子场景、显示位置及试算 |
| ai | AI分析设置 | 场景能力、允许材料/数据、预设分析任务、证据要求、连接状态和权限；无真实模型时明确样例分析模式 |
| data | 数据与运行 | 数据来源目录、字段映射、数据覆盖与准备情况、导入预览、保存状态、集中数据说明、重置业务办理状态及重置配置 |

六个Tab均要有表单/列表/详情及保存后的可见结果，不做只有菜单或“开发中”的空壳。账号密码、真实密钥、实际对外连接不作为本Demo前端配置内容。前端权限用于验证产品行为，不等同真实后端安全控制。

监管子场景是最小业务分类，不再向下建立“子子场景”。一个子场景可以有多条监测规则；一项指标可以关联多个子场景，不为复用指标复制出多个互相不一致的定义。指标公式和汇总口径属于指标定义；业务目标、预警阈值、触发方向、边界条件属于对应目标/规则配置，不能混成一条公式。规则和指标变更经保存草稿、校验/试算、发布后按生效范围/日期使用；历史评估保留当时版本，不用新阈值覆盖过去结论。自动计算定义必须试算通过；专业核查支撑模板校验材料清单、责任人、适用条件和引用完整性，不要求自动算出专业结论才能发布。没有本期业务对象时，可使用明确标为测试输入的合法数据完成校验，不生成实际业务记录或监测结果。

配置与业务使用同一套服务和持久化。刷新保存配置及业务修改，保存失败有明确提示；“重置业务办理状态”只恢复核查整改等业务办理状态，不清除用户配置；“重置配置”单独说明范围并确认后执行。停用已有场景或指标保留历史关联和结果，不物理删除历史记录。配置新增不自动伪造监测结果。导入配置先校验稳定ID与引用，预览新增、变更和冲突后再应用；拒绝孤立引用和静默覆盖。

### 3.7 权限必须作用于整个查询链

总部相关人员按授权查看总部及所有下属单位；所属单位人员只能访问本单位获授权的范围，本级/含下级由授权明确。单位A的含下级授权可以覆盖其A1和A2，不能看到单位B、C。系统管理员的配置维护权限不自动带来全部业务数据权限。

同一授权范围作用于菜单、组织选择树、KPI分子分母、图表汇总、场景五数、列表、对象详情、业务关系、证据、导入/导出及AI分析，不仅隐藏组织树节点。直接路由访问、搜索对象ID、打开已复制链接也需检查授权；关联到范围外对象时，不展示其名称、金额或其他受限信息。处理跨领域同一事项时仍使用统一risk_id，但只展示当前用户有权看到的上下文。

身份或授权变化后重新建立合法的全局组织范围，清理此前抽屉、详情、查询缓存和AI上下文，不能留存上一个总部用户的明细。当前角色和可看范围在界面可理解地显示；角色切换作为验证能力保留，但不称为已实现正式登录或后端鉴权。

### 3.8 区分规则能力与业务数据状态

按投资底稿逐项适配的三类执行方式组织实际规则：结构化自动识别、线索识别与人工复核、专业核查支撑。AI是否辅助按具体材料和任务确定，不把整个线索识别类别等同AI运行。具体字段值读取本版catalog，不用旧40/10/3统计推断自动化程度。采用人工方式的事项可以在线分派、关联材料、记录结论和跟踪整改，不等于不能进入监管平台。

每个对象×规则实例分开判断三件事：①当前业务是否适用；②所需结构化数据或材料是否准备完成；③是否已执行及结论。没有发生该类业务、规则不适用、待数据接入、缺少某份材料、待人工核查、已核查无异常和已发现线索须分别显示；不能统一呈现为“数据不足”，也不能把“不适用”或“未核查”计为合规。

首页只选已具备可靠输入、能解释计算和监管用途的重点指标，完整38项原指标仍在配置目录保留。对暂未具备数据的指标在指标配置/数据来源页说明具体前提，不用几十张缺数据KPI占据首页。可规范中央企业常态数据的展现和补充正常业务样例，但不能通过把异常改成正常来美化结果；本包金额、8件未关闭基准及已约定事实不得随意改写。样例中的异常是待核查线索或管理偏差，不能直接写成已确认违法违规或实际损失。

### 3.9 AI浮动入口与上下文分析

右下角设置“AI分析”浮动入口，点击打开侧边分析面板；入口不遮挡表格操作、分页、提交及关闭按钮。从领域、指标、对象、子场景或规则进入时携带当前组织授权、统计期间、截至日、当前指标/对象/规则、已发布版本和可访问证据。支持“解释当前指标”“梳理命中依据”“对比材料差异”“建议核查问题”等预设任务，给出可点击的事实/材料位置、结论边界和建议。

没有连接真实模型时，面板清楚标注“预置分析”，点击可查看支持任务及实现范围，设置页说明未连接模型服务；只对已配置且具备事实支撑的任务输出内容，任意问题不伪装成即时模型回答。分析不能跨越权限，不能自行认定违法违规、替代专业估值/减值判断，不能自动发布配置、关闭监管事项或执行源系统业务。切换用户后清除不再有权访问的历史分析上下文。

### 3.10 修复指标与计算依据串用

当前版本曾出现点击“投资计划执行率”却展示EAC偏差公式的反馈，必须检查共用详情是否按对象套用了固定示例。指标值、定义、公式、输入、结果、来源记录及目标阈值均绑定当前指标和查询上下文。

计算/追溯查询至少区分`indicatorId + orgNode + period + asOf + definitionVersion + configVersion + dataVersion + authScope`；稳定对象ID也要进入明细查询。不得只按对象查一份固定依据，也不得把不同指标共用的一组演示文本当结果。

验收：总部投资计划执行率8400÷10000＝84%；FA-P001为4800÷6000＝80%；FA-P001预计完工投资偏差率为（11800－10000）÷10000＝18%。三者应显示自己的名称、公式、输入来源和结果。配置发布、统计期间变化、组织切换及身份切换后，值和依据同步刷新。

## 4 复用并补齐共用架构与数据关系

本轮沿用现有工程和技术栈，先定位并复用现有共用能力。只有缺少对应能力时再补齐组件和服务，不重写已有可工作的业务。把数据读取、授权范围、指标计算、规则评估、配置版本、事项状态、关系查询、页面呈现及本地保存分开。

| 共用能力 | 实现要求 |
|---|---|
| 组织与筛选 | 全局组织、含下级/仅本级、期间、截至日改全页；阶段及子视图只改下区；P71内部树选择不改背景全局筛选，关闭恢复来源状态 |
| 对象及关系 | 名称仅展示，连接使用稳定ID；读取seed已有关系及外键；多领域打开同一对象档案 |
| 指标计算 | 从基础明细计算；比例按分子分母合计重算，不能平均百分比；`expected_results`仅用于对照 |
| 场景及阶段 | 读取本版阶段映射；`primary_phase_id`与`associated_phase_ids`各司其职；首页肩形箭头直接联动清单，历史阶段开放问题继续可见；53项投资场景及38项原KRI不复制 |
| 规则与来源 | 展示数据前提、周期、阈值、边界、来源、有效期、版本和证据；区分执行方式、业务适用性、数据准备与执行结论；样例参数与底稿参数分开 |
| 数据追溯 | 展示seed提供的来源名称、业务记录、采集/更新时间及材料位置；指标和上下文共同确定依据，集中数据说明明确合成样例属性；不把Excel列号或研发字段当业务标签 |
| 核查整改 | 统一`risk_id`及状态机制；跨领域引用同一事项，操作日志、说明、期限及证据同步更新 |
| 配置与本地状态 | demo_seed、catalog和configuration_seed通过共用服务初始化；配置发布后页面联动，刷新保留修改；重置业务办理状态与重置配置分别说明范围并确认；业务重置不清除配置，不改原始基线 |

先共同跑通以下对象关系，再扩展领域细节：

- 固定资产投资项目—投资完成/合同成本—达到可使用状态及转固—形成资产—运营与利用—权属或减值核查依据。
- 股权投资项目—被投企业—出资或分红义务—实缴或收款—资金账户；同一企业连到产权快照和产权事项。
- 工程项目—客户及采购合同—成本构成—业务可支付上限/有效批准—支付记录；境外地区、事件、航线、价格敞口关联到同一工程对象。
- 监管事项—相关对象及多领域引用—命中规则—证据—核查整改历史。

投资形成额、资产净值、工程合同额、现金支付及风险关联金额分别统计。同一事实出现在多个领域不倍增金额，也不新增一张独立工单。

## 5 本轮按三批增量实施

| 顺序 | 修改范围 | 本批必要检查 |
|---|---|---|
| 1. 首屏及主路径 | 共用蓝色视觉、侧栏、业务文案、综合总览、固定资产指标与流程、事项详情 | 1440×900及1280×800可读；首页84/8/4/1可点；项目80/90与18%的来源正确，关闭返回不丢状态 |
| 2. 各领域与业务证明 | 股权、国际化、资金、产权、工程差异化展示；跨域同一事项、上下文AI；准备整改材料并完善独立复核 | 本轮脚本路径有对象和依据；材料显式采用；R07可选分支8→7且三个领域同步，原业务金额不变 |
| 3. 配置体验与整体验收 | 六配置页面、指标与规则试算/发布、角色展示、持久化和原型评审 | 业务表单清晰；草稿与生效结果分开；配置真实保存，权限范围正确；业务功能可核对，科技信息部可按功能找到待调研的数据和系统条件 |

统一数据、授权过滤、状态与版本是每一批的基础，第一批就复用并修正需要的底层能力；分批只安排开发顺序，配置与权限同属完整原型范围。业务金额、ID、原53项/38项以及初始8件基准保持不变。

依赖未变不重装，每批做受影响验证及工程必要检查，不为低影响文案重复跑全量测试或录屏。持续完成授权范围内的普通修改，更新实际操作记录，并交付当前有效预览入口。

## 6 必须核对的演示路径与结果

演示业务截至日固定为**2026-06-30**，默认发生额期间为2026-01-01至2026-06-30；文件修订日期不能作为业务日期。金额未另说明均为万元人民币。

首版截至日仅开放已有完整快照2026-06-30，统计期间可按所供事实选择，历史事件窗口独立。核查、整改、复核等监管操作的业务生效日期默认当前演示截至日；实际操作时间另记`recorded_at`，同日操作用`sequence`排序；`closed_at`按演示业务日期记录，不能因电脑在9月操作而漏出6月闭环统计。后续若开放其他截至日，必须重建相应时点的状态和余额，不以当前status或余额冒充历史。期间切换按评估/观察记录确定命中，不能仅凭事项`first_detected`不在所选期间就排除；AC64不要求开放没有快照支持的任意历史截至日。

| 路径 | 必须看到并可点击的结果 |
|---|---|
| 一：投资执行到超概算 | 首页点击84%指标卡打开P71浮层，沿左树到FA-P001的80%、FA-P003的90%，右侧同步显示构成，关闭保持背景；FA-P001预计完工投资11800、偏差18%，五项构成4800+600+3600+800+2000可追溯，进入R01 |
| 二：转固后资产 | 首页点击“运营及后评价”箭头并选“资产运营”，从清单到AS001，再到来源FA-P002；2025年四季度48%、2026年一季度46%作为连续两个完整季度依据；4—6月47%、45%、40%为月度走势；R02当前整改逾期 |
| 三：股权收益及资金 | EQ-P001会计收益200、现金到账80分开；出资到期履约80%、缺口600；分红回收40%、未收120；资金打开同一R03/R04 |
| 四：国际事件及情景 | 事件窗口、交易日和模拟行情标识可查；钢材+10%、运费+25%计算增量48+100=148，工程情景毛利率11.26%；基础预测12%保留 |
| 五：支付核查 | P-PAY001实付1200、有效批准800、业务可支付上限2000；超批准400，本笔未超过业务可支付上限；工程/资金/国际化打开同一R07 |
| 六：产权差异 | JV001同有效期下批准60%、工商模拟快照60%、产权模拟台账55%；R08显示待核实，进入关联EQ-P001及原始快照 |
| 七：工程及事项办理 | ENG-P001设计、采购、建造等并行阶段可点击；收入20000、成本17600、基础毛利率12%；核查、整改提交、复核退回及通过有证据和历史 |

首次加载保留原始8件未关闭事项：红4、黄4、待核查5、整改中3、当前逾期整改1；R09已排除1件保留历史。六领域展示15次，按`risk_id`去重为8件。R07复核通过后全局剩7件，资金、工程、国际化各自同步减少1件。上述数量描述的是恢复种子后的基准，不能在用户操作后强行保持不变。

账户余额6632、受限314.4、可用6317.6；原币、单位、模拟汇率可见。投资完成4800与资金支付4200分别呈现。计划0、合法实际0、缺失值和不适用分开处理；必要事实缺失时显示未评估及具体缺口。

投资目录固定34项固定资产、19项股权，共53项；40次原始KRI定义归并为38个指标，旧版40项结构化监测、10项线索核查、3项专业核查依据仅作历史标签。实际执行按V1.4逐项适配，不据此宣称40项自动运行。补充场景单独标识，不改原目录数量；没有样例事实的指标在配置和数据来源页保留定义及所需输入，不上首页伪造结果。

## 7 验收、预览与最终交付

保留原AC01至AC100要求，保留第23章AC101至AC108原型检查，并增加第24章AC109至AC111资料核对，原型与资料核对范围为AC01至AC111。现有已验证项在行为未受影响时保留既有证据；本轮对新增项和受影响项记录实际结果、证据路径、通过/未通过/未验证及限制，不把旧版一次全通过作为新增配置和权限已通过的证据。

本轮至少实际验证：

- 七业务导航和底部配置入口在1440×900及当前窗口内可读可用，无侧栏溢字、抽屉遮挡；业务文案和匿名命名统一，保留集中合成样例说明。
- 六配置Tab可进入、编辑、校验、保存并刷新恢复；规则/指标按执行方式完成校验或试算、发布与版本留存，专业核查模板不以自动结论作为发布门槛；停用不破坏历史。
- 总部授权全树、单位A授权范围与单位B隔离、系统管理员不自动获得全业务数据；组织树、汇总、对象详情、导出、关系及AI均按同一授权过滤；切换身份清理此前上下文。
- 子场景是最小业务分类，规则可多条，指标跨场景复用；不把配置数量视为实际监测数量。
- 规则适用性、数据准备、执行结论分开；人工核查与AI辅助有材料/结果入口，不用“数据不足”代替执行方式。
- 投资计划执行率总部84%、项目80%的公式、输入和依据正确；EAC偏差18%不串用；配置版本、组织、期间或身份变化后追溯同步。
- 右下AI入口打开上下文面板，预设任务输出带真实样例依据；未连接实时模型明确标识，不伪造任意问答，不跨权限、不自动定性或关闭事项。
- 原指标树关闭恢复、阶段只联动下区、跨域同一risk_id、R07闭环后8变7及受影响领域同步、刷新保存等相关路径保持正确。

计算、权限和状态验证使用有业务价值的断言，样式及文案采用实际页面检查。禁止仅凭静态截图认定联动或权限正确；也无需为低影响文字调整编写镜像测试。临时测试数据与基准分开，验证后恢复正常初始状态。

### 预览交付要求

沿用现有工程实际可用且已授权的预览方式，确认服务启动、页面可打开且必要交互生效。最终必须提供**用户能点击的当前有效完整网址或实际Preview入口**，不能仅提交一段“我已点击测试”的说明。服务在远程环境时，远程内部`localhost`地址不能充当用户浏览器可访问入口；使用当前平台已有的合法转发或预览能力。

不为获得链接擅自创建新的外部隧道、对外部署或接入付费服务。若当前环境确实无法提供可达入口，明确写出卡在哪个服务/环境环节、已完成的本地验证，以及用户需完成的具体操作，不编造地址。无需因录屏失败重复阻塞交付，截图和实际断言可作为相应证据。

### 最终交付

1. 在原工程中完成的源码及必要依赖变更；保留原正常工程组织方式，不另建同功能项目。
2. 更新`README.md`，写清实际验证的启动、构建和检查命令、预览入口、数据与配置保存方式及恢复范围。
3. 更新现有操作说明，补充系统配置、授权范围、AI入口和指标依据修复路径；文档文件名可保留研发用途的“演示”字样，业务UI不得照搬。
4. 更新`验收记录.md`及已查看的关键截图：新增项、受影响项分别据实记录，不把未验证项写为通过。
5. 当前可访问预览入口，和具体到页面/动作/数据范围的尚未完成或受环境限制事项。

本轮前端角色和权限用于产品交互验证，真实生产登录、后端数据鉴权、实时接口、真实模型调用及源系统业务操作仍需正式工程接入，不宣称已完成。对外部署、分享和新建隧道不自动包含在本次改版任务中。

## 8 可直接发给Cursor或Codex的本轮改版指令

```text
继续完善现有海油工程穿透式监管平台Demo，按V1.6校准定位：这是穿透式监管项目的一项系统业务原型交付，用于让领导和业务部门了解未来系统形态、确认业务需求，同时支持与科技信息部调研数据来源、系统衔接及实施方向。

先读当前工程AGENTS.md、《系统原型定位与实施调研说明_V1.6.md》第1至8节和第10至11节，再按涉及页面读取完整业需与必要的数据记录。第9节是操作参考，不是删减业务范围的演示脚本。不要一次加载所有大文件，不重建工程。

保留并完善综合总览、固定资产投资、股权投资、国际化、资金、产权、工程六领域，以及核查整改、系统配置和适用的AI辅助。固定资产在股权前。蓝色主题要稳重精致，修复侧栏文字、溢出和共用详情的业务命名。指标点击打开组织穿透；四流程领域使用简单横向肩形箭头，点击后联动该环节场景与事项，资金/国际化采用专题。

按未来使用方式做完整交互：每项指标可解释口径和构成，每个代表场景有对象、规则及依据，跨域共用事项，整改提交与独立复核有材料和记录。已有金额、编号和正确交互保留，未知不改正常，合成样例性质在数据说明中明确。

系统配置六页都属于原型交付。展现用户与权限、场景层级、规则、指标、AI和数据运行如何维护、试算、保存及生效，不能只作为附带介绍。权限、计算、状态共用基础从每批开始生效。无真实模型时AI面板标注预置分析，明确可用任务和依据，不冒充已连接模型。

同步维护配套实施调研说明：把页面/功能对应到所需字段、对象粒度、建议核实的系统、跨系统匹配条件及待确认问题。现有系统名称、字段、接口、更新频率、模型及部署条件未经确认的写待调研；不宣称已接通，不把访谈任务塞进业务首页，不默认新增基层填报。

开发可按共用框架和固定资产→其他领域及关联/AI/整改→配置细化及总体核对分批推进，但最终按完整原型范围检查。原型实际完成状态与真实系统条件核实状态分别记录。完成后给出可访问预览、已验证/待完善功能和对应调研问题；保留必要检查，不重复无关全量测试或录屏。
```

## V1.4配置契约补充

configuration_seed.json中的目录定义、执行绑定和历史版本分开处理。38项原投资指标均可查定义，4项提供完整参考绑定；其余从现有已验证计算器迁移，找不到才保留草稿，不覆盖已有正常卡片。规则执行必须读取结构化condition和当前版本parameters，不按rule_id保留写死阈值。专业模板通过材料/责任/适用条件校验发布，不要求先得出专业结论。

使用coverage_migration重建旧覆盖候选的显示状态：31条实际监测关联按事实进入计数，其余358条是适用性尚待确认的覆盖规划候选，不能把旧data_insufficient批量渲染为数据不足。候选未经确认不进应评估分母；本期无适用对象是场景汇总空态。

definition_version_archive保留原观察版本，当前配置不能覆盖历史；resource_access_bindings提供源记录及证据的明确授权对象链，无归属默认拒绝。配置域权限与业务域权限独立，防止通过修改本人角色间接扩权。切换用户、配置或数据版本必须清理受影响缓存。

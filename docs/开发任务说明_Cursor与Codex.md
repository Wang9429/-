# 海油工程穿透式监管平台：Cursor与Codex开发任务说明

版本：V1.3｜2026年9月14日｜用途：后续可交互Demo的开发交接

本业需包同时用于Cursor和Codex，业务需求、页面交互、场景目录及示例数据共用同一套Markdown/JSON文件。开发工具由使用者选择；实际完成情况必须由运行、计算及交互验收确认。

本文件交付的是开发任务及验收要求，尚不代表网站已经构建或通过测试。页面中的主体、人员、金额、交易、汇率、行情和分析均使用模拟标识；真实历史事件日期与模拟行情分别说明。

## 1 如何交给Cursor或Codex

两种工具使用同一业需包，无需分别维护两套业务要求。先解压整个业需包，保留全部文件，不只提供主文截图或单段提示词。以下步骤用于后续开发交接，本次交付本身不启动网站开发。

### 使用Cursor

1. 将本包完整放入拟开发的项目根目录，并在Cursor中打开该目录。已有代码工程时，保留原技术栈和现有工作，将业需文件放在可定位的位置。
2. 如果项目根目录已有`AGENTS.md`，将本包的业务约束合并进去，保留原工程约定。然后把第8节开场指令粘贴给开发助手，**显式要求先读取并说明已加载的`AGENTS.md`、全部业需文档及两份JSON**；不依赖特定版本是否自动识别某种指导文件。
3. 助手读取并说明文件位置后，依第5节直接连续完成共同架构、代表路径及各领域细化。让助手直接使用工程文件中的完整内容，避免只依靠聊天摘要实现。
4. 普通实施选择和已授权范围内的修复持续完成。最终依据第7节验收实际运行、计算和交互，并保留具体证据。

### 使用Codex

1. 将本包完整放入拟开发的项目目录，并在可用的Codex入口打开该项目。已有代码工程时，保留原技术栈和现有工作。
2. 将本包的`AGENTS.md`放在项目根目录；如已有同名文件，合并业务约束并保留原工程约定。Codex会读取适用的`AGENTS.md`；从项目根目录开始工作，并先让它说明已加载的指导与需求文件。更具体目录中的指导文件应一并检查。[官方AGENTS.md说明](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
3. 使用第8节相同开场指令，按第5节完成实现和验证。实际可用能力以运行环境为准，不预设模型、订阅或未经验证的安装命令。

### 两种工具共同交付要求

完成后提供可运行项目、经过实际验证的启动步骤、演示操作说明和逐条验收记录。若浏览器或某项执行能力不可用，准确记录未验证项及复现步骤，不将其标为通过。开发中如调整需求文件位置，应同步更新引用；同一事实、指标和监管事项仍使用本包的统一数据与稳定ID。

## 2 输入文件与适用顺序

| 文件 | 用途 |
|---|---|
| 海油工程穿透式监管平台_完整业需_V1.3.md | 产品定位、页面、监管链路、规则、统一计算、演示路径与AC01至AC70验收要求 |
| 公开产品借鉴与页面设计_V1.3.md | 公开产品参考的适配结论、信息组织和页面设计要求 |
| 投资底稿适配对照_V1.2.md | 投资底稿逐行取舍、原KRI、口径修正、来源及阶段映射；本次沿用V1.2 |
| investment_catalog.json | 53项投资子场景、38项原KRI、稳定ID、原文与阶段归属 |
| demo_seed.json | V1.3统一组织、业务对象、事实明细、关系、数据追溯、阶段、证据、风险与验收基准；业务事实及阶段映射沿用V1.2 |
| 开发任务说明_Cursor与Codex.md | 实施顺序、开发要求和交付方式 |
| AGENTS.md | 在代码迭代中持续遵守的项目约束 |

先完整读取两份V1.3文档，再落实投资适配与JSON。产品行为以完整业需为准，公开产品参考服务于本平台的监管任务；投资原文取舍以适配对照为准，稳定ID以JSON为准。投资适配附件V1.2和catalog的V1.2阶段映射继续适用；V1.3仅补充工具交接与视觉要求，原53项场景、38项KRI及其原文口径保留。按本版首页直接联动和P71浮层交互实现，不沿用旧版必须进入流程Tab或独立指标页的操作。

不能把`original_columns`中的源系统审批、支付拦截、资产处置或处罚动作直接改造成本平台的操作。遇到实质冲突，记录文件、条目、影响和采用口径；按已明确的修正实现，不将未知数据补成正常。

## 3 产品必须保持的主线

一级导航顺序固定为：**综合总览、固定资产投资管理、股权投资管理、国际化业务、资金管理、产权管理、工程项目管理**。导航次序改变不要求重编既有页面编号、对象ID或路由。

顶部增加“监管工作台”P77入口，汇聚当前用户待核查、待整改、待复核及督办事项，不增加第八个侧栏业务领域。共用“数据追溯”P78展示指标或规则对应的业务记录及来源，“业务关联”P79展示当前对象的上下游及跨领域关系，均从指标、对象或事项上下文进入。

平台是从总部及所属单位开展监管的工作台。用户应能从组织及指标找到对象，沿业务链查看上下游关系，解释规则为何命中，打开对应材料，再开展本平台的核查、整改和复核。源系统的投资审批、招采、签约、付款、记账、正式产权办理和责任追究只展示事实、结果及来源。

需要同时实现三种进入路径：

| 进入路径 | 用户要获得的结果 |
|---|---|
| 组织及指标穿透 | 从海油工程总部到二级、三级单位及项目/资产/账户等末端对象，查看构成和业务明细 |
| 业务链及对象联动 | 从同一投资项目、资产、被投企业、合同、付款或产权事项，查看关联领域中的同一对象及依据 |
| 场景及规则追溯 | 从阶段或专题进入场景，查看适用条件、口径、阈值、版本、命中事实及材料位置，进入核查整改 |

组织管理树与法人/产权关系分开。二级直管项目直接挂接，不补造三级；被投企业不因有股权关系自动进入管理组织树。对象缺少关系时显示缺口，不构造虚假链路。

### 3.1 首页直接完成阶段选择

领域首页顺序为“标题及全局筛选—领域重点指标—横向肩形箭头/专题—场景执行清单及对象事项”。固定资产、股权、产权、工程四领域都在监管总览首页中间直接显示可点击的横向肩形箭头。每个箭头只放**阶段名称＋未关闭事项数**，名称完整可读，不放对象数、场景数、数据覆盖、进度百分比或状态堆叠。

点击任一箭头，直接更新本页下方的场景执行清单及其对象、事项。清单提供监测对象、命中对象、未关闭、本期已整改闭环、逾期整改数五个数量入口。没有先切换“流程监管”Tab或进入矩阵的前置步骤。详细核查表、项目时间线或阶段资料作为选中阶段下的可选展开内容，不替代首页箭头和清单。

全局组织、含下级/仅本级、期间及截至日改变时全页按同一口径重算；阶段及阶段子视图仅筛选下方区域。顶部KPI明确标注“当前组织及期间·领域范围”，不随阶段点击偷偷变成阶段KPI。下区显示当前阶段标签，可清除恢复该领域全部阶段；切换全局范围后仍适用的阶段可保留并重算，无对象时显示具体提示。

固定资产八阶段依次为：**储备谋划、可研论证、决策审批、设计概算、采办管理、建设实施、验收投用、运营及后评价**。“运营及后评价”下区提供“资产运营/投资后评价”子切换，默认资产运营；投用后资产持续运营，后评价按适用条件及评价时点开展，不能实现为先完成后评价才能运营。股权、产权、工程节点名称及条件性、并行关系按完整业需V1.3及对应模板，不自行编造业务硬流程。资金继续使用专题，国际化继续使用地区/事件和影响对象。

箭头顺序表达业务监管视角，不等同所有项目必须串行经过每一节点。工程允许设计、采购、建造等并行；条件性节点按对象适用性显示，说明位于条下或选中阶段的详细区域。已完成或历史阶段只要仍有关联未关闭事项，节点和下方清单就保留这些问题，不仅取对象当前阶段。

### 3.2 指标点击打开P71大抽屉或弹窗

点击首页指标卡默认打开P71大抽屉或大弹窗，保留背景首页及其状态。浮层顶部显示指标、单位、期间、截至日、当前范围及关闭入口；左侧为“海油工程总部—二级单位—三级单位—项目”的可展开树，右侧显示选中节点的指标值、目标/偏差、分子分母、构成和对应业务明细。账户、资产、被投企业等按指标实际末端对象接续；缺层按真实关系跳过。

选择树节点只改变浮层中的指标范围，不修改背景页全局组织。右侧明细可继续查看数据追溯P78、业务关联P79及证据，保留返回P71的节点和展开状态。关闭浮层或完成返回后，背景页面的组织、期间、阶段/子视图、清单筛选、页码、滚动位置保持原样。保留P71页面编号；允许直接链接作为补充入口，但不以整页跳转替代指标卡的默认浮层体验。

### 3.3 五个数量的统计边界

观察归属统一按业务窗口结束日`window_end`落入所选期间（含端点），且评估取得时间不晚于截至日（按主文的业务取得/生效时间口径，不能用实际操作日志`recorded_at`替代）；季度或滚动窗口不按重叠月份拆分分摊。监测记录按实际评估关联的阶段进入清单，不因catalog把场景关联到多个阶段就自动铺满监测对象。有效历史命中即使最新评估正常仍保留；只有确认误报等有效排除才移出命中。人工核查有明确线索可以计命中，无结论显示“—”。整改截止日缺失显示“缺期限”，不当作未逾期。

| 数量 | 统计与点击要求 |
|---|---|
| 监测对象 | 当前范围内已实际评估至少一条适用规则或已完成人工核查的唯一对象；按对象类型＋对象ID去重。应评估与已评估分开，部分规则完成仍显示部分覆盖；点击查看监测记录与缺口 |
| 命中对象 | 当前观察期自动规则命中或人工评估发现明确线索的唯一对象；含已整改闭环的命中历史，确认误报并排除的不计入；同一对象多次命中去重，按对象类型显示并可查看依据 |
| 未关闭 | 当前范围截至日状态未关闭且未排除的唯一risk_id；进入同一事项清单 |
| 本期已整改闭环 | 当前范围内所选期间复核通过关闭且截至日仍为closed、close_reason=rectified的唯一risk_id；整改提交、待复核、已排除均不计入 |
| 逾期整改数 | 当前范围内已进入整改责任范围、截至日仍未关闭（含待复核）且超过有效整改截止日的唯一risk_id；待复核不自动停表；核查或复核其他办理SLA超期不混入 |

资产运营子视图默认按资产数，投资后评价子视图按项目数；末阶段箭头将两者的未关闭事项按risk_id合并去重。未关闭/逾期整改数是截至日存量，本期已整改闭环是期间内完成且截至仍关闭（close_reason=rectified）的数量，不用“本期已整改闭环÷（未关闭＋本期已整改闭环）”计算闭环率。

核查、复核等其他办理SLA超期在监管工作台单列，不计入上述第五数。

场景行、阶段汇总和领域汇总各自按所属范围去重，不能直接相加场景行或阶段行形成上级数。项目、资产、付款等不同类型不混算成一个模糊“对象总数”；按主文明确的类型或分项展示。历史阶段问题和跨阶段关联按规则/事项关联纳入，不因项目已进入下一阶段就消失。

### 3.4 全平台采用统一蓝色主题

本Demo面向国央企总部监管人员，视觉定位为稳重、精致、清晰的企业管理系统。高端感通过统一排版、适度留白、清楚的层级、整齐数字和细致交互实现。综合总览、六业务领域、监管工作台及共用详情全部使用同一套主题和组件。

| 元素 | 设计要求 |
|---|---|
| 一级侧栏 | 深蓝`#0B1F3A`，白色或足够清晰的浅色文字；选中入口清晰可辨，固定资产保持在股权上方 |
| 主色及交互 | 主蓝`#1D5FD1`用于主要按钮、链接、选中指示及重点业务数据；交互悬停、键盘焦点和禁用状态分别设置 |
| 页面与卡片 | 页面浅灰蓝`#F3F6FB`，卡片白色`#FFFFFF`，细边框和轻阴影；保持统一圆角、卡片间距和内容对齐 |
| 文字与数值 | 深色正文清晰可读；金额和百分比位数一致，金额列右对齐，单位及期间明确；不用装饰性字体或小号浅灰文字承载关键内容 |
| 指标与流程 | KPI突出值、单位和必要比较；横向肩形箭头继续保持阶段名＋未关闭数，选中状态用蓝色边框或描边，监管风险状态仍使用独立红黄语义 |
| 图表与详情 | 常规比较采用有限层次的蓝色系列；风险红、黄、正常绿、未评估灰分别配文字/图标。指标浮层、组织树、场景表、规则、证据和整改窗口沿用统一视觉 |

蓝色表达品牌及交互，不表示风险正常。不得把高风险改成蓝色、仅靠色彩区分状态，或把选中阶段的蓝色误当成风险等级。首页以可用的监管工作台为核心，不添加无实际监管用途的旋转地球、发光网格、霓虹描边、满屏渐变或装饰动画。不得编造官方标识或授权背书。

在1440×900优先验证完整业务入口、重点指标、流程/专题和场景清单的衔接。四流程领域首屏能看到横向箭头；阶段名称清晰完整，空间不足使用受控横向滚动，不压成难读的小字或复杂折线。P71和其他浮层的标题、关闭、树和明细均可操作。视觉验收按主文AC65至AC70执行，原AC01至AC64全部保留。

## 4 先建立共用架构与数据关系

优先沿用现有项目技术栈；无工程时采用可维护的TypeScript组件化前端，具体库按运行环境选择。把数据读取、指标计算、规则评估、事项状态、关系查询、页面呈现及本地保存分开。

| 共用能力 | 实现要求 |
|---|---|
| 组织与筛选 | 全局组织、含下级/仅本级、期间、截至日改全页；阶段及子视图只改下区；P71内部树选择不改背景全局筛选，关闭恢复来源状态 |
| 对象及关系 | 名称仅展示，连接使用稳定ID；读取seed已有关系及外键；多领域打开同一对象档案 |
| 指标计算 | 从基础明细计算；比例按分子分母合计重算，不能平均百分比；`expected_results`仅用于对照 |
| 场景及阶段 | 读取本版阶段映射；`primary_phase_id`与`associated_phase_ids`各司其职；首页肩形箭头直接联动清单，历史阶段开放问题继续可见；53项投资场景及38项原KRI不复制 |
| 规则与来源 | 展示数据前提、周期、阈值、边界、来源、有效期、版本和证据；演示参数与底稿参数分开 |
| 数据追溯 | 展示seed提供的源系统标识、业务记录、采集/更新时间及材料位置；模拟来源明确标注，缺失来源如实提示 |
| 核查整改 | 统一`risk_id`及状态机制；跨领域引用同一事项，操作日志、说明、期限及证据同步更新 |
| 本地演示状态 | 首次从种子初始化，刷新保留修改；保存失败可见；重置确认后恢复种子，不改原始基线 |

先共同跑通以下对象关系，再扩展领域细节：

- 固定资产投资项目—投资完成/合同成本—达到可使用状态及转固—形成资产—运营与利用—权属或减值核查依据。
- 股权投资项目—被投企业—出资或分红义务—实缴或收款—资金账户；同一企业连到产权快照和产权事项。
- 工程项目—客户及采购合同—成本构成—业务可支付上限/有效批准—支付记录；境外地区、事件、航线、价格敞口关联到同一工程对象。
- 监管事项—相关对象及多领域引用—命中规则—证据—核查整改历史。

投资形成额、资产净值、工程合同额、现金支付及风险关联金额分别统计。同一事实出现在多个领域不倍增金额，也不新增一张独立工单。

## 5 分阶段实施和检查点

| 顺序 | 完成内容 | 检查点 |
|---|---|---|
| 1. 共同架构 | 七个一级入口、全局筛选、组织树、稳定对象、统一指标和事项状态，以及统一蓝色主题组件 | 导航按新顺序；组织、日期、对象和risk_id贯通；种子基准可从明细算出；先确立深蓝侧栏、浅灰蓝底和白卡的排版规范 |
| 2. 六领域代表路径 | 六领域首页、P71指标浮层及共用场景/对象/事项详情 | 四流程领域首页肩形箭头直接联动下区五数；资金/国际化保持专题；P71树与构成可操作并恢复背景，跨域返回保持上下文 |
| 3. 固定资产细化 | 八阶段及运营/后评价子视图，34场景、投资与成本口径、利用率和R02 | 路径一、二可讲解；验收投用延伸至持续运营；季度依据和月度走势分开；总体正常可定位异常资产 |
| 4. 股权细化 | 19场景、会计收益、现金回报、出资与分红义务、产权关联 | 路径三可讲解；收益不重复相加；R03/R04在资金领域保持同一记录 |
| 5. 其余领域细化 | 国际事件窗口与影响测算、资金支付核查、产权同有效期比对、工程并行阶段 | 路径四至七可讲解；R07、R08、工程情景及对象关系真正联动 |
| 6. 交互及边界收口 | 导入预览、导出、角色范围、状态保存与恢复、全部验收、截图检查 | AC01至AC70逐条记录；修复可复现缺陷；提供真实运行方法和限制 |

不要先把单一领域做成孤立成品，再为其他领域复制数据和页面。每阶段可保存代码检查点并记录差异。后续按领域修订时，先定位变化对共用数据、筛选、计算及跨领域路径的影响，完成受影响部分的回归。

## 6 必须核对的演示路径与结果

演示业务截至日固定为**2026-06-30**，默认发生额期间为2026-01-01至2026-06-30；文件修订日期不能作为业务日期。金额未另说明均为万元人民币。

首版截至日仅开放已有完整快照2026-06-30，统计期间可按所供事实选择，历史事件窗口独立。核查、整改、复核等监管操作的业务生效日期默认当前演示截至日；实际操作时间另记`recorded_at`，同日操作用`sequence`排序；`closed_at`按演示业务日期记录，不能因电脑在9月操作而漏出6月闭环统计。后续若开放其他截至日，必须重建相应时点的状态和余额，不以当前status或余额冒充历史。期间切换按评估/观察记录确定命中，不能仅凭事项`first_detected`不在所选期间就排除；AC64不要求开放没有快照支持的任意历史截至日。

| 路径 | 必须看到并可点击的结果 |
|---|---|
| 一：投资执行到超概算 | 首页点击84%指标卡打开P71浮层，沿左树到FA-P001的80%、FA-P003的90%，右侧同步显示构成，关闭保持背景；FA-P001预计完工投资11800、偏差18%，五项构成4800+600+3600+800+2000可追溯，进入R01 |
| 二：转固后资产 | 首页点击“运营及后评价”箭头并选“资产运营”，从清单到AS001，再到来源FA-P002；2025年四季度48%、2026年一季度46%作为连续两个完整季度依据；4—6月47%、45%、40%为月度走势；R02当前整改逾期 |
| 三：股权收益及资金 | EQ-P001会计收益200、现金到账80分开；出资到期履约80%、缺口600；分红回收40%、未收120；资金打开同一R03/R04 |
| 四：国际事件及情景 | 事件窗口、交易日和模拟行情标识可查；钢材+10%、运费+25%计算增量48+100=148，工程情景毛利率11.26%；基础预测12%保留 |
| 五：支付核查 | P-PAY001实付1200、有效批准800、业务可支付上限2000；超批准400，本笔未超过业务可支付上限；工程/资金/国际化打开同一R07 |
| 六：产权差异 | JV001同有效期下批准60%、工商模拟快照60%、产权模拟台账55%；R08显示待核实，进入关联EQ-P001及原始快照 |
| 七：工程及事项办理 | ENG-P001设计、采购、建造等并行阶段可点击；收入20000、成本17600、基础毛利率12%；核查、整改提交、复核退回及通过有证据和历史 |

首次加载保留原始8件未关闭事项：红4、黄4、待核查5、整改中3、当前逾期整改1；R09已排除1件保留历史。六领域展示15次，按`risk_id`去重为8件。R07复核通过后全局剩7件，资金、工程、国际化各自同步减少1件。上述数量描述的是恢复种子后的基准，不能在用户操作后强行保持不变。

账户余额6632、受限314.4、可用6317.6；原币、单位、模拟汇率可见。投资完成4800与资金支付4200分别呈现。计划0、合法实际0、缺失值和不适用分开处理；必要事实缺失时显示未评估及具体缺口。

投资目录固定34项固定资产、19项股权，共53项；40次原始KRI定义归并为38个指标，40项结构化监测、10项线索核查、3项专业核查依据。补充场景单独标识，不改原目录数量。对没有种子事实的指标提供定义和拟来源，不伪造自动识别结果。

## 7 验收与最终交付

按完整业需的AC01至AC70建立验收记录，写明实际结果、证据路径、通过/未通过/未验证和限制。既要验证页面，也要验证同一操作引起的数据与状态变化。独立边界输入测试完成后恢复种子，不能把临时测试事项混入初始8件。

原AC01至AC64业务验收保持不变，新增AC65至AC70视觉验收。蓝色主题不能覆盖风险语义或改变业务流程。

截图和交互验收至少包括：

- 统一蓝色主题在全部领域、P71浮层、P77工作台、P78数据追溯、P79业务关联及整改详情中一致；检查对齐、留白、正文/数字清晰度、可点击状态和风险配色。
- 七个入口在1440×900下的首屏；固定资产、股权、产权、工程的指标及横向肩形箭头可见，每个箭头只含阶段名和未关闭数，表格和数字可读。
- 点击指标卡打开P71大抽屉/弹窗，左树下钻与右侧构成同步；关闭恢复背景全部状态。首页直接点阶段联动下区五数与清单，阶段选择不改顶部领域KPI，全局组织及期间变更则全页重算。
- 固定资产八阶段、运营/后评价子切换、历史阶段开放问题、条件性节点和工程并行场景；详细核查表可选展开，不以Tab或矩阵作为必经步骤。
- 同一对象跨领域切换及返回、规则版本与证据阅读，五数去重、复核关闭计数和逾期整改子集。
- R07在三个领域打开同一记录；核查、整改、复核退回、通过后的状态与数量变化；刷新与重置结果。
- 事件窗口和压力测算、产权同有效期对照、错误导入与有效预览、筛选结果导出及角色范围。
- 无业务、数据不足、来源缺失、无匹配记录等提示。操作后检查布局溢出、遮挡、错误提示、不可点击入口及控制台错误。

可使用现有可用浏览器/测试工具执行并保存截图；不能仅依据静态截图宣布筛选、导出或状态流程通过。构建和有业务价值的自动化检查围绕计算、数据关联、状态转换及AC边界进行，不为静态文案编写重复测试。

最终交付包含：

1. 完整源码、实际依赖与锁文件；沿用已有工程的正常组织方式。
2. `README.md`：已验证的环境前提、依赖准备、启动、构建与验证命令、实际本地访问入口、保存和重置方式。必须在工程建立后填写实际命令。
3. `演示操作说明.md`：七条路径的入口、点击顺序、预期数值、模拟数据说明及恢复初始状态的方法。
4. `验收记录.md`：AC01至AC70实际结果、计算核对、浏览器交互及截图证据索引。
5. `screenshots/`：经过查看的关键页面和交互结果截图，文件名能辨认页面或验收动作。
6. 已知限制和下一步建议：具体到页面、动作、数据范围或计算，不以“核心功能完成”掩盖缺口。

正式系统接入、实时行情、生产登录与权限、真实模型调用不属于本Demo交付；本平台监管办理使用本地演示状态。发布、外部分享及生产接入按届时用户明确范围执行，开发完成不自动等同已部署。

## 8 可直接发给Cursor或Codex的开场指令

> 请在当前项目根目录中，基于海油工程穿透式监管平台业需包，实现可运行、可交互的监管工作台Demo。先读取并说明已加载的AGENTS.md、海油工程穿透式监管平台_完整业需_V1.3.md、公开产品借鉴与页面设计_V1.3.md、投资底稿适配对照_V1.2.md、investment_catalog.json、demo_seed.json及开发任务说明_Cursor与Codex.md。一级导航严格按综合总览、固定资产投资管理、股权投资管理、国际化业务、资金管理、产权管理、工程项目管理排列。先实现共同架构与六领域代表性路径，再依次细化固定资产、股权及其余领域。首页指标点击用P71大抽屉或弹窗，左组织树右指标构成，关闭恢复背景。固定资产、股权、产权、工程首页中间用横向肩形箭头，每箭头只显示阶段名与未关闭数，点击直接联动下方场景清单及监测对象/命中对象/未关闭/本期已整改闭环/逾期整改数五数。阶段及子视图仅改变下区，全局组织期间改变全页，顶部KPI明确领域范围。采用主文V1.3阶段映射、条件性与并行关系，不把详细表或流程Tab设为必经步骤。全部页面采用统一的国央企蓝色主题：深蓝侧栏#0B1F3A、主蓝#1D5FD1、浅灰蓝背景#F3F6FB、白色卡片，以排版、留白和组件细节体现稳重精致；风险红黄语义独立，禁止用蓝色替换风险状态。重点完成业务链对象跨域联动、规则证据追溯及核查整改，保留53个投资场景、38项原KRI、稳定对象ID、统一risk_id和seed口径。指标从明细计算，expected_results仅核对；所有模拟数据明确标识。源系统业务审批、付款和处置仅展示事实结果。优先沿用现有技术栈，自行处理常规实施选择并持续完成实现。实际运行项目，执行AC01至AC64业务验收、AC65至AC70视觉验收及七条演示路径，检查1440×900首屏与交互结果截图，修复已发现缺陷，最终提供经验证的运行方法、演示说明、验收记录、截图和具体未实现/未验证项。

后续按领域调整可以使用：

> 继续在现有Demo上细化【领域】。先读取V1.3业需中该领域及共用交互要求，列出此次修改涉及的页面、对象关系、指标和验收项，然后直接实现。沿用共用组织筛选、稳定ID、统一risk_id及状态机制；完成受影响跨领域路径和数值回归，更新截图、运行说明及验收记录。

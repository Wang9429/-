# 海油工程穿透式监管平台｜UI视觉整改说明

版本：V1.6.1（视觉增量）  
依据：用户提供的综合总览实际截图、已确认的V1.6业务需求。  
用途：交给Cursor在现有工程中实施UI改版，服务领导展示、业务确认和科技信息部调研。

本文件是现有业需的视觉补充。业务计算、授权范围、流程顺序、场景规则和整改状态继续执行V1.6。随附改版参考图用于对齐外观，字段、数据和交互以本文件及既有业需为准。参考图不是已运行网站的截图。

## 1. 这张截图具体暴露了什么

| 实际可见现象 | 对使用的影响 | 本轮处理 |
|---|---|---|
| 深蓝侧栏上出现深蓝菜单文字，选中项也不清楚 | 用户难以找到各领域入口 | 在共用导航组件修复文字与背景、图标及交互状态 |
| 顶部“监管工作台”“场景规则库”“数据依据”等控件被挤成逐字竖排 | 入口无法快速识别，顶栏挤入指标区域 | 拆分全局工具栏与业务筛选栏，控件设置合理宽度和换行规则 |
| 标题层次弱，正文和辅助说明整体显得过小 | 首屏像一张缩小的清单，领导难以快速读到重点 | 恢复页面标题、清晰的字号层次、卡片主次关系 |
| 首页顶部六张卡片并列，含“可判定数据覆盖8.59%” | 技术性说明占据业务关注位置 | 按V1.6恢复四张核心指标卡；覆盖说明进入数据与运行 |
| 六领域卡片都有多组数字、小字、长句及编号 | 信息拥挤、视觉主次不清 | 每卡一个主指标、一项未关闭数、一个关注点、一个进入入口 |
| 百分比呈现为“84.00%%”“62.50%%”“12.00%%” | 显示错误影响可信度 | 统一数值格式化函数，组件与数据层只追加一次单位 |
| 出现P20/P10/P30等内部页面编号、risk_id、演示单位名称 | 暴露研发字段，不适合业务展示 | 页面使用业务名称；内部ID继续保留在实现和追溯数据中 |
| 品牌区出现“演示版V1.3” | 与Cursor所述V1.6成果不一致，需要核实 | 核对正在服务的工程、构建与入口；版本和样本说明统一进入数据说明 |

截图只能证明当时页面的显示状况。不能仅凭V1.3标识断言业务代码仍是旧版本，也不能从截图确认抽屉、权限、系统配置或AI功能是否可用。

## 2. 先确认修改会落到用户正在看的页面

Cursor先检查现有工程中负责综合总览、侧栏、顶栏、全局主题的实际组件，以及当前预览启动命令与端口。

1. 确认用户正在打开的路径、43917端口对应的进程及工作目录，与正在编辑的工程一致。
2. 识别当前使用开发服务还是生产预览。生产预览修改后按项目脚本重新构建并启动，使新样式真正进入当前入口。
3. 如保留构建版本信息，在“数据说明”或“关于”中展示实际版本信息，用于核对，不能只把页面标签改成V1.6.1来宣称构建已更新。
4. 浏览器刷新后检查实际生效样式。菜单蓝字的候选原因包括全局链接颜色、组件自带样式、主题变量、内联颜色和样式覆盖顺序，需从实际元素确认。
5. 复用原工程的预览方式。当前工作首先完成UI，美化后再按用户希望提供其他电脑可访问的网页入口；真实网址应来自实际部署或转发结果。

## 3. 确定的整体视觉

采用深蓝侧栏、白色顶部工具栏、浅灰蓝页面背景、白色卡片。高端感来自可读性、排版、留白、对齐和交互细节。

| 项目 | 统一规格 |
|---|---|
| 侧栏背景 | `#0B1F3A` |
| 侧栏默认文字 | `#E7EFFA` |
| 侧栏选中背景／文字 | `#164B8E`／`#FFFFFF` |
| 侧栏图标 | `#B8C9E3`，选中时白色 |
| 主交互色 | `#1D5FD1` |
| 页面背景 | `#F3F6FB` |
| 内容卡片 | `#FFFFFF`，细边框`#E3EAF3` |
| 主文字／次文字 | `#182B49`／`#66768D` |
| 风险提示 | 红`#C7383B`、深黄`#946000`，同时配文字标签 |
| 圆角 | 卡片10px，按钮及输入框6–8px |
| 阴影 | 轻微，例如`0 2px 10px rgba(17,43,77,.04)` |
| 字体 | 系统无衬线，优先PingFang SC、Microsoft YaHei；数字等宽对齐 |
| 页面标题 | 24px、600，行高约34px |
| 区块标题／卡片标题 | 16–18px、600 |
| 正文／菜单 | 正文14px以上，菜单15px |
| 辅助信息 | 12–13px，用于单位、期间或补充说明，不能承担主要结论 |
| KPI主数值 | 30–34px、600，单位14px |
| 页面边距／区块间距 | 24px／20–24px |
| 卡片内部／卡片间距 | 20px／16px |

状态使用文字和颜色共同表达。选中蓝色仅表达交互状态，高风险仍保留红色语义。图标采用现有同一图标库的线性图标，18–20px，避免混入彩色表情。页面不增加无数据依据的趋势图或环比，不添加大幅装饰横幅、霓虹、旋转地球或虚构官方标志。

## 4. 共用框架：优先改这里，让所有页面一起改善

### 4.1 左侧边栏

- 展开宽度固定248px，`flex-shrink: 0`；主内容必须能够收缩，宽表格不能挤窄侧栏。
- 品牌区分两行：“海油工程”“穿透式监管平台”。第一行18px，第二行13px，左右20px，区块高度约88px。
- 导航外边距12px，单项高度48px，水平内边距12px，图标与文字间隔10–12px。选中项使用有内边距的圆角浅蓝底，配2–3px左侧标识。
- 菜单次序：综合总览、固定资产投资管理、股权投资管理、国际化业务、资金管理、产权管理、工程项目管理。标准菜单名称在正常展开状态完整单行显示。
- “系统配置”作为独立底部区，保留清晰图标与文字；导航区域必要时纵向滚动，底部入口不遮挡最后一个领域。
- 默认、访问过、悬停、选中、键盘聚焦状态逐一设置。仅给父容器白色文字可能不足，需确认实际链接元素颜色。
- 不能通过缩小字体、全局裁切或随意扩大侧栏掩盖问题。超长自定义名称才考虑提示或内部换行。

### 4.2 顶部工具栏与筛选区

将截图中拥挤的一行拆成两种用途：

| 区域 | 内容及布局 |
|---|---|
| 顶部工具栏 | 高56–60px，左侧面包屑，右侧监管工作台及待办数、数据说明、当前用户菜单 |
| 页面标题区 | “综合总览”及简短副标题“总部及所属单位监管情况” |
| 业务筛选区 | 组织范围、统计期、截至日；完整名称水平排列，控件高36–40px |

组织选择的选中值同时呈现“海油工程总部（含下级）”，避免把“含下级”挤成孤立的窄按钮；所属单位账号只显示其有权限的范围。

组织控件建议260–300px，统计期180–220px，截至日140–160px，控件之间留16px。可用宽度不足时，整个筛选项换到下一行，标签与控件作为一组。按钮文字禁止逐字换行，用户菜单限制最大宽度，不占满半条顶栏。

场景规则入口归入系统配置相应页面；计算依据从指标或事项上下文进入；已有“重置业务办理状态”“重置配置”入口收纳到“系统配置—数据与运行”。入口调整应保持原有功能可达和授权校验。

## 5. 综合总览重新排版

1440px宽优先采用：标题及筛选 → 一句关注摘要 → 四张指标卡 → 六领域3×2卡片 → 重点事项 → 单位风险分布。页面自然向下滚动，不通过整体缩放塞进一屏。

### 5.1 四张核心指标卡

| 卡片标题 | 初始样本显示 | 辅助信息 | 点击 |
|---|---|---|---|
| 固定资产投资计划执行率 | 84% | 完成8,400／同期计划10,000万元 | 指标组织穿透抽屉 |
| 未关闭监管事项 | 8件 | 待核查5／整改中3 | 当前范围的未关闭事项 |
| 其中高风险事项 | 4件 | 未关闭事项中的高风险 | 当前范围的高风险事项 |
| 逾期整改事项 | 1件 | 需跟进整改 | 当前范围的逾期整改事项 |

卡片高度约128–144px。标题在上，数字为视觉中心，辅助说明在下，右上使用小型操作标识即可。风险用小面积标签与数字强调；不要把每卡都画成粗蓝边。

纳管单位数、项目数可在范围说明或对象清单查看；可判定数据覆盖转入数据与运行并核对V1.6适用性口径。不能把未确认适用性的大量候选配置描绘成几百项业务数据异常，也不能改成100%掩盖真实情况。

上述数字仅是恢复初始样本后的预期结果，页面必须从原统一数据和权限范围计算。整改、组织或期间变化后，卡片及摘要同步更新。

### 5.2 六领域卡片

取消当前“外层大白卡＋内层六张白卡”的重复包裹，直接在页面背景上排列六张独立卡，区块标题位于其上。每卡约174–190px高，统一四个位置：领域名称与小图标、一个主指标、未关闭数及关注点、进入领域。

| 顺序 | 标题 | 主指标／初始值 | 未关闭 | 关注点 |
|---|---|---|---|---|
| 1 | 固定资产投资管理 | 投资计划执行率84% | 2件 | 预计完工投资偏差 |
| 2 | 股权投资管理 | 年度投资计划完成率62.5% | 3件 | 到期出资履约 |
| 3 | 国际化业务 | 境外在管工程项目1个 | 3件 | 航线与成本敞口 |
| 4 | 资金管理 | 可用资金6,317.6万元 | 3件 | 支付授权核查 |
| 5 | 产权管理 | 权益信息待核实差异1项 | 1件 | 多来源权益信息核对 |
| 6 | 工程项目管理 | 预计完工毛利率12% | 3件 | 成本预测与目标差距 |

每卡主指标标签13px、数值24–28px；主标题16px。领域标题和“进入领域”进入该领域；卡内有独立指标按钮时，其点击只打开对应指标，避免嵌套点击触发两次跳转。

区块右上或底部用一句“跨领域关联事项合并计数”，详细说明在展开提示中说明8件事项与15次领域引用。页面不展示risk_id或按单元格相加等研发解释。

### 5.3 重点事项和单位分布

重点事项默认显示三条有依据的记录：预计完工投资偏差、资产利用偏低及逾期整改、境外工程预计毛利偏差。每行包含关注事项、对象或单位、关键事实、当前进展、查看详情。表格行高约56px，问题标题14px，状态为清晰的小标签。

原单位风险矩阵保留，位于重点事项后或“单位分布”展开区。显示“下属二级单位A/B/C”等规范名称。列宽及表头需统一，“无业务”“未评估”“本次监测未发现异常”保留真实区别。

百分比、金额、数量交给统一格式化方法：输入为数值、单位由一层负责显示；84%或84.00%均应与所在指标展示精度一致，始终只有一个百分号。金额使用千分位，单位紧跟数值并在同一行内对齐。不同指标按既有口径保留合理精度，不通过取整改变判断。

## 6. 同步应用到领域页、抽屉、配置和AI

| 页面组件 | 视觉和布局要求 | 保留的行为 |
|---|---|---|
| 四个流程领域 | 重点指标下方保留简洁横向肩形箭头；阶段名与未关闭数完整显示；选中蓝色、未选中浅底细边 | 点击阶段更新下方场景与执行数量，顶部全领域KPI不随阶段缩小口径 |
| 资金与国际化 | 采用专题或事件入口，外观与流程导航协调 | 各自专题、市场影响及压力测算，不强加审批流程 |
| 指标组织穿透抽屉 | 宽度约视口80%–90%；左侧组织树约30%，右侧指标事实约70%；标题24px，树节点14px以上 | 总部—二级—三级—项目按实际层级展示；穿透不修改背景页全局组织 |
| 详情弹窗 | 首屏先呈现关键事实、关注原因、当前进展、相关依据；更多计算及规则折叠 | 多层返回、Esc关闭最上层、关闭后保持树节点和背景筛选 |
| 系统配置 | 左侧栏框架保持统一，六个Tab正常横排；白色表单区，标签和输入框对齐，保存区位置稳定 | 用户与权限、监管场景、监测规则、指标管理、AI分析设置、数据与运行均有实际页面 |
| AI分析 | 右下48px左右入口，距离边缘24px；面板420–480px；短标题、清晰事实和来源 | 继承当前上下文和权限；未连接模型时标明预置分析，引用可打开 |

较小窗口优先合理重排并允许容器内滚动。浮层关闭按钮、表单保存按钮、右侧表格操作不被AI入口遮挡。

## 7. 可参照的共用CSS结构

以下是布局与颜色的实现示意。Cursor应映射到项目现有组件、CSS变量或主题配置，类名按实际工程调整。先定位现有样式覆盖关系，再合并到共用组件；不要简单将此片段粘贴后就假定修复生效。

```css
.reg-app {
  --nav-bg: #0b1f3a;
  --nav-text: #e7effa;
  --nav-active: #164b8e;
  --brand: #1d5fd1;
  --page: #f3f6fb;
  --text: #182b49;
  --muted: #66768d;
  --border: #e3eaf3;
  display: flex;
  min-height: 100dvh;
  color: var(--text);
  background: var(--page);
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC",
    "Microsoft YaHei", sans-serif;
  font-size: 14px;
  line-height: 1.5;
}
.reg-app, .reg-app * { box-sizing: border-box; }
.reg-sidebar {
  flex: 0 0 248px;
  width: 248px;
  height: 100dvh;
  position: sticky;
  top: 0;
  display: flex;
  flex-direction: column;
  background: var(--nav-bg);
}
.reg-nav { flex: 1; min-height: 0; overflow-y: auto; padding: 12px; }
.reg-sidebar a.reg-nav-link,
.reg-sidebar a.reg-nav-link:visited {
  display: flex;
  align-items: center;
  min-height: 48px;
  gap: 12px;
  padding: 10px 12px;
  margin-bottom: 4px;
  border-radius: 8px;
  color: var(--nav-text);
  text-decoration: none;
  font-size: 15px;
  font-weight: 500;
}
.reg-sidebar a.reg-nav-link:hover { background: #12345b; color: #fff; }
.reg-sidebar a.reg-nav-link[aria-current="page"] {
  background: var(--nav-active);
  color: #fff;
  box-shadow: inset 3px 0 #5c9cf2;
}
.reg-nav-link:focus-visible { outline: 2px solid #a9d2ff; outline-offset: 2px; }
.reg-nav-link svg { flex: 0 0 18px; width: 18px; height: 18px; }
.reg-nav-label { min-width: 0; color: inherit; white-space: nowrap; }
.reg-sidebar-footer { flex-shrink: 0; padding: 12px; border-top: 1px solid #263c58; }
.reg-main { flex: 1; min-width: 0; }
.reg-toolbar {
  min-height: 60px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 12px 24px;
  background: #fff;
  border-bottom: 1px solid var(--border);
}
.reg-toolbar-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 12px; }
.reg-toolbar-actions button { flex-shrink: 0; white-space: nowrap; }
.reg-content { padding: 24px; }
.reg-filters { display: flex; flex-wrap: wrap; align-items: center; gap: 12px 16px; }
.reg-filter-item { display: flex; align-items: center; gap: 8px; min-width: 0; }
.reg-filter-item label { flex-shrink: 0; white-space: nowrap; }
.reg-card {
  min-width: 0;
  padding: 20px;
  background: #fff;
  border: 1px solid var(--border);
  border-radius: 10px;
  box-shadow: 0 2px 10px rgba(17, 43, 77, .04);
}
.reg-kpis { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 16px; }
.reg-domains { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px; }
.reg-value { font-size: 32px; font-weight: 600; font-variant-numeric: tabular-nums; }
.reg-table-wrap { max-width: 100%; overflow-x: auto; }
@media (max-width: 1199px) {
  .reg-kpis, .reg-domains { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .reg-content { padding: 20px; }
}
@media (max-width: 899px) {
  /* 调用现有可访问的导航折叠组件；此处不以裁切文字替代折叠功能。 */
  .reg-toolbar { flex-wrap: wrap; }
  .reg-kpis, .reg-domains { grid-template-columns: minmax(0, 1fr); }
}
```

如果图标或菜单内部文字有独立颜色，需同时修正。若项目采用组件库，优先修改其主题和组件样式；避免无范围的全局链接改色影响主内容链接。CSS动画仅用于轻微悬停和打开过渡，并尊重减少动画设置。

## 8. 最小验收与本轮交付

在用户实际打开的入口完成以下检查：

1. **综合总览整页**：侧栏所有菜单清楚完整；顶栏不逐字竖排；显示四项主指标及六领域卡；百分号只有一个；没有内部页面编号、risk_id或演示单位名称。
2. **固定资产投资页面**：字体和卡片沿用新主题，流程箭头可见；打开84%指标能看到组织树，再点击一个阶段确认联动仍可用。
3. **系统配置页面**：六个Tab及表单可读、侧栏底部配置入口可达，保存与关闭按钮无遮挡。
4. **代表浮层**：指标抽屉和叠加的计算依据窗口保持可读；Esc只关闭最上层。AI浮窗不遮挡操作。
5. **视口**：1440×900、1280×800以及浏览器125%缩放检查侧栏、顶栏和主区溢出。较窄有效宽度可改为两列并自然滚动，不能整体缩放或压小字体。

提供综合总览、固定资产及指标抽屉的实际运行截图，并说明运行入口、启动方式及本轮修改的共用组件。图像生成的参考图不能用作运行验收截图。自动化浏览器受限时，可以人工检查并提供真实截图；未完成的检查如实标注。

验证按此次改动影响开展。UI修改优先检查渲染与代表性点击；若变更了统一格式化函数，补查百分比、金额和数量显示。业务自检保留现有必要检查，不为单次配色调整反复全量测试。

## 9. 直接发给Cursor的执行指令

```text
请读取《海油工程_UI视觉整改说明_V1.6.1.md》，结合改版参考图，在当前海油工程穿透式监管平台工程中完成UI整改。

先核对当前预览入口对应的工程和构建，再修复共用侧栏、顶栏、主题与布局，随后按说明重新排版综合总览，并将统一样式应用到六领域、系统配置、指标抽屉和AI面板。

本轮重点：侧栏浅色文字且菜单完整；顶栏按钮水平排列；正文清晰；四张主指标卡＋六领域卡＋重点事项；清理P20等内部编号和演示单位名称；统一百分比格式，解决重复百分号。

业务计算、组织权限、流程环节及整改联动沿用V1.6。页面数值继续从统一数据按权限计算，参考图中的数字只用于对齐初始样本。

完成后在我实际使用的预览入口检查，给我综合总览、固定资产页面及指标抽屉的真实截图和可点击入口。浏览器自动检查未完成时明确标注，服务返回200与编译成功分别报告，不替代视觉验收。
```

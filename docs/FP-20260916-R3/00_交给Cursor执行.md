# Cursor执行指令｜FP-20260916-R3

请在当前海油工程穿透式监管平台工程上执行本包的资金管理、产权管理增量改造，直接完成实现、受影响路径验证和原GitHub Pages发布，不停留在计划或静态设计稿。

## 先读与定位

1. 阅读 `01_资金产权完整优化业需_R3.md`、`02_80条子场景专题环节映射_R3.md`、`05_验收清单_R3.md`。
2. 读取 `03_配置契约与迁移_R3.json`、`04_80条子场景配置参考_R3.json`。它们是需求和映射参考，不是可覆盖应用数据的seed。
3. 检查当前分支、既有指标ID、组织树、80条场景、配置存储和GitHub Pages工作流。用户报告基线是功能 `4ae94ac` / 发布 `4df4f8c`；以实际最新工程为准，不回退提交、不另起网站、不清空用户配置。
4. 将工程原业需相关章节同步更新，以R3明确替代的内容为准；保留无冲突既有业务口径和证据历史。

## 必须实现

一、资金首页使用盈利能力、资产负债状况、资金流动性三类，每类最多4张现有主指标卡。首页取消完整各单位对比表；适用指标卡中增加紧凑小趋势，详情展示同源完整趋势。无需增加独立大趋势区。

二、指标详情左侧复用综合总览组织树，右侧展示当前值、可比变化、趋势、直属下级比较、构成、关联事项和依据。可切换指标必须同时满足当前领域、当前分类、已启用、入口允许、可计算及有权限。从营业收入进入不出现资产负债或流动性指标。切换保留单位、期间、截至日；末级对象不适用时只回到合法范围内适用组织；背景页面不变。

三、趋势按配置和有效历史决定是否显示。流量用期间发生额，余额用期末值，比率逐期计算。主卡半年累计与月度图分别标明简短时间口径。净资产收益率不默认年化，不能用半年利润除以每月净资产伪造月度收益率。缺数据不补零、不随机、不倒填；无适用趋势收起图区，停用指标则隐藏整卡及候选项。产权四项统计有历史快照时可显示趋势，静态股比、控制结论不强行配图。

四、资金六专题保留，完整映射40子场景。每专题有适用且已启用的代表性场景和可操作路径；尚无运行/核查能力的定义不批量启用。S011捐赠付款归资金收付；借入与出借、融资与结算用途分清。通用场景用有条件关联入口，不复制事项。

五、产权下区先选四专题：产权交易、产权登记、标识与名称资质、股权与控制权。仅产权交易下出现经济行为及对应肩形流程；其他专题直接场景执行。流程随已确认经济行为模板，不将一条通用顺序说成所有业务法定流程。同一法人多笔交易分别有进度。40条子场景以显式映射归属，登记义务可与交易末端共用记录。

六、两领域专题/流程下立即是“场景执行情况”：标题旁当前标签、紧凑统计、按一级场景分组的子场景执行清单。删除独立大块“当前环节”和重复摘要，不把场景表埋在大卡片后。点击子场景或统计可到对象、正常/命中依据、核查整改；专业核查需有材料及结论操作。专题切换只更新下区。

七、保留并打通产权价款—资金到账、担保—被担保企业、出借—借款方及回收三类关联。使用同一业务ID和同一问题case_id，返回保留上下文，权限不扩大。配置页同步指标分类/趋势配置、场景主专题/主环节/业务类型/条件路由，保存启停真正作用页面。

## 必须保留

- 综合总览的版式、标题、指标选用和现有交互；真实办理引起的统计正常联动。
- 其他领域行为、既有授权、蓝色视觉、浮层层级及关闭返回。
- CASH2-S039默认容差0，没有精度依据时拒绝非零/非法保存发布，不静默改值；1200/800/2000分别保留原含义。
- 7152/834.4/6317.6账户口径与352万元差异待核实；不得编造调节项。
- 产权60/60/55证据差异、历史评估版本、新旧整改统一统计与历史截至日还原。
- 80条原场景定义及ID、已有用户配置和办理记录。新增样例必须自洽，不虚构公司真实违规，不为保截图数字写死结果。

## 实施顺序与验收

先统一配置及映射，再改资金卡片/抽屉/趋势，再改资金场景区与产权导航，再补齐代表路径，最后验证与发布。按 `05_验收清单_R3.md` 核对受影响路径即可，不反复重跑无关全站工作。

请实际操作：盈利能力切换与末级穿透、首页与详情同一时间点读数、无趋势示例、资金6专题、产权4专题及流程显隐、场景到依据/办理、跨域返回、配置开关；抽查1440/1280无整页溢出。不把路由200代替操作验证。

沿用现有GitHub Pages（`/-/`）发布，分别报告实现提交与已发布提交、工作流状态和公网核对结果。不能只说“推送GitHub成功”。无法验证大陆网络时写明待用户实测，不迁移Vercel或临时隧道。

交付：修改清单、受影响验收结果、未完成项、已更新公网入口、当前构建实际截图ZIP及源码可下载附件。截图要能看到实际各专题与穿透效果，不能只给本地`/opt/cursor/artifacts`路径。禁止将需求已写入或配置有定义冒充功能已实现。

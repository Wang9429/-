# 验收记录

验收环境：Linux 沙箱，Node 22，Next 16.3.5 生产构建，端口 43917，Chrome 无头浏览器 1440×900。
数据基准：`data/demo_seed.json` v1.3，演示截至日 2026-06-30，期间 2026-01-01 至 2026-06-30。

## 验证手段

| 手段 | 命令 | 结果 |
| --- | --- | --- |
| 计算口径自检（48 项断言） | `npm run verify` | 48 通过 / 0 失败 |
| 浏览器交互冒烟（24 项断言） | `node scripts/ui-smoke.mjs` | 24 通过 / 0 失败，无控制台报错、无 404 |
| 类型检查 | `npx tsc --noEmit` | 无错误 |
| 规则检查（含 React Compiler） | `npm run lint` | 无错误、无告警 |
| 生产构建 | `npm run build` | 14 条路由构建成功 |

`npm run verify` 直接调用页面使用的同一批计算函数（`lib/metrics.ts`、`lib/monitoring.ts`、`lib/risks.ts`），与种子中的 `expected_results`、`expected_stage_results_v1_2` 比对，不读取任何页面硬编码值。

## AC 逐条结果

标注说明：**通过** 指有自动断言或实际浏览器操作证据；**已实现待人工确认** 指功能与数据已按业需实现并可在界面操作，但本次未编写对应自动断言；**未验证** 指本次环境未覆盖。

### 一、导航与页面结构

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC01 | 通过 | 渲染文本中一级菜单顺序为综合总览、固定资产、股权、国际化、资金、产权、工程；P20 卡片在 P10 之前 |
| AC02 | 通过 | 1440×900 视口下固定资产页首屏可见 6 张 KPI 与 8 段肩形箭头，箭头文案仅「阶段名 + 未关闭 N 件」 |
| AC03 | 通过 | 冒烟断言「点击建设实施后下区统计」与「顶部 KPI 口径未被阶段选择缩小」；下区显示「当前环节：建设实施」可清除标签 |
| AC04 | 通过 | `verify` 第 8 组：EQ-P001 / EQ-V12-04 业务状态 completed，同时保留未关闭事项 R08 |
| AC05 | 已实现待人工确认 | 工程模板 ENG-V12-02/04/05/06 计划与实际日期重叠，页面按重叠区间展示 |
| AC06 | 通过 | 资金与国际化页使用专题卡片而非生命周期箭头（`flowMode="topics"`） |

### 二、指标穿透与计算

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC07 | 通过 | 冒烟断言组织树含二级单位、点击三级单位甲一可下钻到 FA-P001；二级单位乙直接挂 FA-P003，无虚构三级 |
| AC08 | 通过 | 演示二级单位丙显示「—」与「当前范围无业务」，不显示 0% 或绿灯 |
| AC09 | 通过 | `verify`：总部 84%、FA-P001 80%、FA-P003 90%；按分子合计÷分母合计计算，非 85% |
| AC10 | 通过 | `verify`：同类资产总体利用率 60%，AS001 明细单列且标红 |
| AC11 | 通过 | FA-P001 明细中投资完成 4800 与资金支付 4200 分列 |
| AC12 | 通过 | `verify`：FA-P001 偏差 18%，EAC 构成合计 11800，五项互不重叠，P78 可查每项来源 |
| AC13 | 已实现待人工确认 | 缺失预测项按 `dataComplete=false` 输出缺口说明与已知下界，不按 0 计算 |
| AC14 | 未验证 | 阈值边界（95% / 100% / 100.01%）需改写演示参数后重测，本次未执行 |
| AC15 | 未验证 | 关键路径 59/60 天边界未执行 |
| AC16 | 未验证 | ±5% 边界未执行 |
| AC17 | 已实现待人工确认 | 计划 0、null、有效 0 分别对应「计划外线索」「数据不足」「有效 0 值」三种展示 |
| AC18 | 已实现待人工确认 | 对象档案中投用状态、转固日期分列，资产运营、权属、后评价入口可达 |

### 三、场景目录与股权

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC19 | 通过 | 场景规则库页按 `investment_catalog.json` 展示 34 固定资产 + 19 股权，结构化/线索/核查依据分别打标 |
| AC20 | 通过 | 原 KRI 页显示 38 项，重复定义共用指标 ID |
| AC21 | 已实现待人工确认 | 经营偏离与估值兑现分别定义方向语义，未套同一颜色方向 |
| AC22 | 通过 | `verify`：EQ-P001 会计收益率 9.09% 与现金到账 80 分列，不叠加 |
| AC23 | 通过 | `verify`：出资履约 80%（缺口 600）、分红回收 40%（未收 120） |
| AC24 | 已实现待人工确认 | 集中度展示同时给出样本量与结构参考说明 |

### 四、事项状态与闭环

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC25 | 通过 | `verify`：全局 8 件、六领域合计 15 次、去重后 8 件；R07 在三领域为同一记录 |
| AC26 | 通过 | 冒烟实操：提交整改 → 复核通过后，工程页未关闭由 3 降为 2，综合总览由 8 降为 7 |
| AC27 | 通过 | `verify`：截至日仅 R02 逾期；R09 已排除不计入当前风险与逾期 |
| AC28 | 通过 | R07 详情分别展示超本笔批准 400 与未超 2000 累计可支付，两条规则结果独立 |
| AC29 | 通过 | 产权页 JV001 同有效日期并列 60/60/55，标注待核实 |
| AC30 | 通过 | `verify`：余额 6632、受限 314.4、可用 6317.6 |
| AC59 | 通过 | 同 AC26：提交后仍未关闭且本期闭环不增，复核通过后未关闭 −1、本期闭环 +1 |
| AC60 | 已实现待人工确认 | 排除不计本期闭环；重开恢复未关闭并保留历史动作（界面可操作，未写自动断言） |
| AC61 | 通过 | `verify` 第 8 组，同 AC04 |

### 五、国际化、导入导出与角色

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC31 | 已实现待人工确认 | 事件窗口显示事件日期、交易日数与「真实事件日期 + 模拟行情」标识 |
| AC32 | 通过 | `verify`：钢材 48 + 运费 100 = 148 万元，情景毛利 11.26%，基础 12% 不被覆盖 |
| AC33 | 已实现待人工确认 | 历史事件仅作对比重放，页面明示不认定 2026 项目真实遭遇 |
| AC34 | 已实现待人工确认 | 数据依据页导入模板含错误行原因与有效行预览 |
| AC35 | 已实现待人工确认 | 导出 CSV 含截至日、组织范围与模拟标识 |
| AC36 | 通过 | 冒烟实操：重置后恢复种子 8 件基准；刷新保留本地修改（localStorage） |
| AC37 | 通过 | 角色为总部领导/配置管理人员时办理按钮禁用并提示，总部领域管理人员可复核 |
| AC38 | 已实现待人工确认 | 规则与证据均标注来源、版本与待核实说明 |

### 六、工作台、关系与联动

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC39 | 已实现待人工确认 | 总览 KPI 点击进入工作台并带入筛选 |
| AC40 | 通过 | 冒烟断言：组织树 → FA-P001 → 80%，P78 显示 11800/10000 与源记录 |
| AC41 | 已实现待人工确认 | R07 关联 ENG-P001—SC001—P-PAY001，三领域同一记录 |
| AC42 | 已实现待人工确认 | 管理树、产权关系、业务关联分别呈现，边标注关系名与来源 |
| AC43 | 通过 | 全站无新建审批/付款/处置按钮，仅展示事实与依据 |
| AC44 | 已实现待人工确认 | 督办仅写入本地记录并提示「未向外部发送」 |
| AC45 | 已实现待人工确认 | 页面结构对应公开产品借鉴文档的工作台、组织钻取、来源追溯与关系穿透 |
| AC46 | 通过 | 本文件即启动与演示路径执行记录；冒烟脚本覆盖总览、固定资产流程、指标下钻、事项办理 |
| AC47 | 通过 | 冒烟断言：抽屉打开且底页阶段与滚动未被重置 |
| AC48 | 通过 | 冒烟断言：抽屉内下钻后底页仍为「海油工程总部（总部·含下级）」；Esc 可关闭 |
| AC49 | 已实现待人工确认 | 股权、产权、工程首页箭头直接刷新下区，无需切流程 Tab |
| AC50 | 已实现待人工确认 | 全局组织/期间/截至日同时更新顶部 KPI 与下区 |

### 七、阶段细分与五数口径

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC51 | 通过 | 固定资产 8 环节齐备；FA-P002 运营中与后评价待开展并存 |
| AC52 | 通过 | 冒烟断言：切到运营及后评价后五数由 2/1/1/0/0 变为 2/1/1/0/1；资产运营按资产数、后评价按项目数 |
| AC53 | 通过 | `verify`：交割完成仍有分期出资未完成，缺口 600 仅对到期义务 |
| AC54 | 已实现待人工确认 | 未到退出条件不显示退出逾期；后评价不强制进入退出 |
| AC55 | 已实现待人工确认 | 产权五类模板切换后箭头与场景随事项类型变化，无实例显示无事项 |
| AC56 | 已实现待人工确认 | 工程阶段可重叠，阶段性结算早于最终验收 |
| AC57 | 通过 | `verify`：FA-S20 场景命中对象 1、未关闭事项 1；规则种类单列，金额不翻倍 |
| AC58 | 通过 | `verify`：六领域合计 15 次去重为 8 件；60 个阶段的箭头数逐一比对一致 |
| AC62 | 通过 | 下区「覆盖说明」显示应监测 3 / 已监测 2 / 部分覆盖，数据不足 36 条、不适用 1 条 |
| AC63 | 已实现待人工确认 | 五类数量均可点击「明细 ›」展开并导出，列表去重数等于点击数 |
| AC64 | 通过 | `verify` 中未关闭按截至日存量统计，不因 `first_detected` 不在期间而剔除 |

### 八、视觉验收

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| AC65 | 通过 | 全站色值集中在 `app/globals.css`，六领域共用同一主题 |
| AC66 | 通过 | 1440×900 无头浏览器截取页面文本与布局，KPI、箭头、场景区均可见，无截断 |
| AC67 | 通过 | 建设实施、运营及后评价环节同时显示蓝色选中态与红色风险点；数据不足显示灰态而非绿色 |
| AC68 | 已实现待人工确认 | 组件字号与间距统一在 `components/ui.tsx`，正文 14px、辅助 12px |
| AC69 | 已实现待人工确认 | 归一化行情图使用图例与线型区分，无 3D 与发光装饰 |
| AC70 | 通过 | 冒烟实操覆盖点击、树选择、Tab 切换与 Esc 关闭；Esc 只关闭最上层浮层 |

## 本轮修复的缺陷

1. **指标穿透抽屉内的 P74 / P78 / P79 入口指向不存在的路由**（`/objects/...`、`/risk-cases/...`、`/business-links/...`、`/data-trace/...`），点击会 404。已改为在浮层内继续打开对象档案、事项办理与数据追溯，符合「不跳离背景首页」的要求。
2. **Esc 会同时关闭弹窗和其下方的抽屉**。已引入浮层栈，Esc 只关闭最上层，关闭数据追溯后仍回到原树节点。
3. **`npm run dev` 在拦截 HMR WebSocket 的环境中页面不水合**，表现为页面可见但点击全部无响应。已定位为 Next 16 Turbopack 开发客户端的连接依赖，非应用代码问题；演示改用 `npm run preview` 生产构建，并写入 README。
4. React Compiler 报出的 `setState in effect` 与记忆化告警已按 `key` 重挂载与 `useMemo` 修正，`useSyncExternalStore` 承载演示状态，消除水合不一致。

## 未验证事项

- AC14、AC15、AC16 的阈值边界测试（95%/100%/100.01%、59/60 天、±5%）需要临时改写演示规则参数，本轮未执行，恢复种子后应单独进行。
- 标注为「已实现待人工确认」的条目功能已实现且可在界面操作，但本轮未编写自动断言，建议按《演示操作说明》逐条人工走查。
- 未截取图片存档（沙箱以文本与断言方式验证），`screenshots/` 目录未建立。

# 海油工程穿透式监管平台项目约定

本文件用于Cursor现有Demo工程的V1.6项目原型增量改版，也适用于Codex接续开发。用户已经开发并试用网站，本轮直接修改现有工程，保留技术栈、稳定ID、业务金额和已有可用能力，不重建网站。投资原53项子场景、38项KRI及原文审计依据保留，规则落地方式按V1.4重新适配。本文件指导代码修改，业需文档更新不代表代码已完成改版或通过验收。

## 参考文件与读取范围

1. `海油工程穿透式监管平台_完整业需_V1.6.md`
2. `公开产品借鉴与页面设计_V1.6.md`
3. `投资底稿适配对照_V1.4.md`
4. `investment_catalog.json`
5. `demo_seed.json`
6. `configuration_seed.json`
7. `开发任务说明_Cursor与Codex.md`
8. `系统原型定位与实施调研说明_V1.6.md`

上述文件按需查询。首次先读本AGENTS、开发任务说明第8节及《系统原型定位与实施调研说明_V1.6.md》第1至8节及第10至11节，再定位受影响代码；操作示例和材料按需读第9节，科技信息部调研读第10至11节。后续按相关主业需章节、投资场景/指标ID和必要配置记录检索，不反复全文加载大MD/JSON。已有工程约定需合并保留。

产品行为以完整业需为准；产品参考用于适配页面与交互；投资取舍以适配对照为准；JSON中的稳定ID和关联关系不得任意重编。若目录布局不同，查找实际文件并记录位置。实质冲突记录到开发说明，遵从用户已明确的修订口径；不得把数据缺失改成正常。

## V1.6项目交付定位

- Demo是项目的一项系统业务原型交付：领导了解建设方向，业务确认功能和口径，科技信息部据页面调研数据、系统衔接及实施条件。
- 综合总览是总部领导首页：监管主体全景（纳管单位、在管项目、高风险责任单位、未关闭高风险及逾期高风险）加专项领域监管概况；固定资产投资计划执行率只出现在固定资产领域卡。数字按当前授权与筛选计算，总部在范围内计入纳管单位；不得把缺失当 0，不得无预警即标“正常”。覆盖规划、待接入与技术日志下沉配置，影响当前结果的具体缺口仍可见。
- 先完成综合总览、固定资产、指标抽屉和事项详情，再贯通各领域/AI/整改，后完善配置体验。授权数据和状态机制从每批开始生效。
- 保持原样本金额和数量，使用单位B等已有对照，不宣称全面合规、全公司真实覆盖、真实损失追回或已上线成效。讲解脚本只在文档，不塞入业务页面。
- 新的合成整改材料由操作人显式采用并保存为新记录，附授权对象关系，不把草稿自动当已执行事实；独立复核前不得关闭。R07原始1200/800/2000与差额400保持不变。

## 产品结构与边界

- 一级导航顺序：综合总览、固定资产投资管理、股权投资管理、国际化业务、资金管理、产权管理、工程项目管理。
- 核心路径：组织及指标下钻；业务链中的对象跨领域联动；场景、规则、来源与证据追溯；本平台核查、整改和复核。
- 平台承担监管展示、分析与跟踪。投资审批、招采、签约、付款、记账、资产处置、正式产权办理及责任追究只展示源系统事实与结果。
- 不把底稿原文或`original_columns`中的执行动作直接实现为本平台操作。不展示没有作用的“审批”“支付”“处置”按钮。
- 一级领域分开，其他监管视图可使用Tab。监管事项和对象等共用详情从领域进入；侧栏底部独立增加“系统配置”P76，路由`/settings`。“重置业务办理状态”“重置配置”和集中数据说明归入“数据与运行”页面，不把配置入口仅放在顶部。
- 顶部“监管工作台”用于个人核查、整改、复核及督办事项；保留七个业务入口并与底部配置分区。数据追溯、业务关联从指标、对象或事项进入并保留来源上下文。P71—P79仅用于研发定位，业务标题和按钮不得显示这些页面编号。
- 首页直接完成阶段监管选择，不要求先切流程Tab或查看矩阵。详细核查表、阶段材料和项目时间线可从当前阶段区域选择展开；其他监管视图按完整业需配置。
- 固定资产、股权、产权、工程四领域首页中间显示简单横向肩形箭头，1440×900首屏可见指标和流程。每箭头只含完整阶段名称＋未关闭事项数；点击直接联动下方场景执行清单。资金继续专题，国际化继续地区/事件及影响对象。
- 阶段业务状态和监管事项状态独立；历史或已完成阶段仍保留未关闭问题。箭头顺序不是强制串行流程，条件性节点及工程并行关系在条下或详情说明，不塞入箭头。

## 首页、指标浮层与统计

- 全局组织、含下级/仅本级、期间及截至日改变全页。阶段和子视图仅筛选下区，顶部KPI明确标注当前组织及期间的领域范围；阶段选择不能偷偷缩小顶部KPI口径。下区显示可清除的阶段标签。
- 指标卡点击默认打开P71大抽屉或大弹窗，不跳离背景首页。左侧为总部—二级—三级—项目的树，右侧显示当前节点的指标、分子分母、目标/偏差、构成及明细；按实际指标接续资产、账户等末端。
- P71内选择组织只影响浮层，不能修改背景全局组织。继续查P78/P79或证据后可回到原树节点；关闭恢复背景组织、期间、阶段、子视图、筛选、页码和滚动位置。
- 固定资产八阶段：储备谋划、可研论证、决策审批、设计概算、采办管理、建设实施、验收投用、运营及后评价。末阶段下区默认“资产运营”，可切“投资后评价”；资产运营按资产数、投资后评价按项目数，末箭头未关闭按risk_id合并去重。
- 资产投用后持续运营，后评价按适用条件及评价时点开展；不能设置为先后评价再运营。股权、产权、工程阶段名称、适用条件和并行关系按完整业需V1.6及JSON模板，禁止自行猜写硬流程。
- 下区五数为监测对象、命中对象、未关闭、本期已整改闭环、逾期整改数。前两项按对象类型＋对象ID去重，分别展示项目、资产等类型；后三项按risk_id去重，不能把对象数与事项数混用。
- 监测对象仅包含已实际评估至少一条适用规则或已完成人工核查的对象；仅纳入计划或资料未评估不计入。应评估范围与已评估范围分别可查，完成部分规则仍显示部分覆盖。
- 观察归属统一按业务窗口结束日`window_end`落入所选期间（含端点），且评估取得时间不晚于截至日（按主文的业务取得/生效时间口径，不能用实际操作日志`recorded_at`替代）；季度或滚动窗口不按重叠月份拆分分摊。监测记录按实际评估关联的阶段进入清单，不因catalog把场景关联到多个阶段就自动铺满监测对象。有效历史命中即使最新评估正常仍保留；只有确认误报等有效排除才移出命中。人工核查有明确线索可以计命中，无结论显示“—”。整改截止日缺失显示“缺期限”，不当作未逾期。
- 命中对象包含当前观察期内自动规则命中或人工评估发现明确线索的对象，即使相关事项已整改闭环仍保留命中历史；确认误报并排除的不计命中。当前观察期及人工核查口径按主文显示。
- 未关闭和逾期整改数按截至日统计；逾期整改数是未关闭事项的子集。本期已整改闭环只统计所选期间复核通过关闭、且截至日仍为closed、close_reason=rectified的事项；整改提交、待复核、已排除不计入。
- 不用“本期已整改闭环÷（未关闭＋本期已整改闭环）”计算闭环率，分子是期间流量、未关闭是时点存量。
- 同一对象跨规则去重，同一事项跨场景/阶段去重；阶段和领域汇总独立按范围计算，不相加清单行。历史阶段问题通过阶段/场景关联纳入，不只按对象当前阶段筛选。

## 蓝色主题与视觉实现

- 全平台使用统一的国央企企业系统风格：稳重、精致、清晰；通过排版、留白、组件一致性和细节体现品质。综合总览、六领域、工作台、浮层、详情及整改操作使用同一主题。
- 深蓝导航`#0B1F3A`，主蓝`#1D5FD1`，页面浅灰蓝`#F3F6FB`，卡片白色`#FFFFFF`。主题色、字体、间距、圆角、边框、阴影及状态色在共用配置中统一维护，不逐页面手写不同风格。
- 展开侧栏默认248px（统一可调240—264px），`flex: 0 0 248px`，不被主区压缩；主区`flex: 1; min-width: 0`。导航字15px、默认`#E7EFFA`、选中白色，深蓝背景`#0B1F3A`；图标固定宽度不缩小，文字容器`min-width: 0`。标准菜单完整可读，超长自定义名称在栏内换行或省略并可见全名提示，不溢出、不用缩小字号掩盖布局错误。
- 侧栏上部菜单可独立滚动，底部配置保持可达；品牌标题也需在宽度内排版。主蓝用于交互及重点业务数据；卡片细边框、轻阴影和适度留白，不满屏重渐变、发光或装饰动画。
- 风险红、黄、正常绿及未评估灰保留独立语义，并带文字/图标。蓝色不等于正常；流程选中用蓝色边框或描边，不能覆盖节点的风险含义。
- 金额列右对齐、百分比位数一致、单位和期间明确，正文和辅助说明清晰可读。图表常规序列用有限蓝色层次；风险标识按业务语义独立配置。空值、无业务、未评估等仍按业务规则显示。
- 横向肩形箭头只保留阶段名与未关闭数；空间不足采用受控横向滚动，不压小字号、截掉关键名称或改成复杂折线。1440×900首屏能看到四流程领域的指标和箭头。
- P71指标浮层、组织树、规则、证据、数据追溯及整改窗口统一样式；键盘焦点、关闭、悬停、禁用和错误状态清楚。关闭恢复背景状态的要求保持不变。
- 不添加无实际监管用途的旋转地球、发光网格或霓虹描边，不编造官方标识或授权背书。布局以分析、定位问题和办理核查整改为目的。
- 实际查看关键页面截图，保留主文AC01至AC70并落实AC71至AC100新增要求。每批验证受影响项及项目必要检查，不因样式改动无理由重跑全量；截图不替代计算、权限和交互验证。

## 本轮实施顺序

按开发任务说明第5及第8节：第一批共用视觉、综合总览、固定资产原型路径；第二批六领域差异化、跨域关联、AI与核查整改；第三批配置体验和原型评审。每一批共用同一授权、计算、版本与状态，不能用分批为由遗漏底层约束。

沿用工程技术栈和依赖。持续完成已授权普通修改，检查受影响交互及必要工程检查；依赖未变不重装，没有具体风险不重复全量测试或录屏。最终交付当前可访问预览及简短实际验证记录。

## 实施调研与交付资料

- 六领域、配置和适用AI同属原型完整范围，不因领导交流时长删除页面。功能须能被业务人员自主点击核对。
- 配套说明按页面/指标/子场景关联所需字段、粒度、来源候选、匹配条件和待确认问题。系统、接口、更新频率、模型与部署条件未核实的保留待调研。
- 原型交互已验证不等于真实系统已联通；两类状态分开。配套调研记录不渲染成业务首页模块，不默认增加基层填报。
- 业务部门确认口径和责任，科技信息部核实实现条件；反馈关联到业需版本。原型AC不替代生产接口、性能、安全与运维验收。

## 系统配置与持久化

- 系统配置为侧栏底部入口P76，`/settings`；固定六Tab：`users`用户与权限、`scenarios`监管场景、`rules`监测规则、`indicators`指标管理、`ai`AI分析设置、`data`数据与运行。
- 六页都必须有真实可操作的列表、表单、校验和保存结果，按需求提供试算、发布、停用或预览，不做“开发中”空壳。无需前端维护真实账户密码、服务密钥或未经授权的外部连接。
- `configuration_seed.json`与catalog、demo_seed通过共用服务初始化。配置与业务页面读取同一状态，刷新保留保存结果，失败可见。既有本地数据迁移应保留业务办理历史和用户已保存内容。
- 监管子场景是最小业务分类；一级监管场景包含多个子场景，一个子场景可配置多条规则；不得为规则再建立子子场景层级。
- 指标可关联多个子场景，使用一份稳定定义。指标名、单位、公式、输入字段、汇总口径、频率和显示位置可配置；目标值、阈值、比较方向与边界放目标/规则配置，不混在公式中。
- 配置按草稿、校验/试算、发布和生效范围/日期管理，保留历史版本及结果采用的版本；保存草稿不立即改变首页统计，发布后相关查询刷新。自动计算定义需试算通过；专业核查支撑模板仅需校验材料清单、责任人、适用条件及引用完整性，不要求自动产生专业结论。无本期对象可使用明确标为测试输入的合法数据完成校验，不生成实际业务记录或监测结果。停用不删除历史场景、规则、指标关联及结果。
- 配置新增或阈值变化不能伪造监测记录、自动关闭已有事项或改写历史命中。试算与正式采用结果分开；如需重评，应保存新的评估记录与版本。
- 数据页展示来源、字段映射和覆盖/准备情况，提供导入预览、明确的数据说明、重置业务办理状态及重置配置。导入配置先校验稳定ID和引用，列明新增/变更/冲突后再应用；拒绝孤立引用或静默覆盖。两个重置动作分别说明影响范围并确认，业务办理状态重置不得清除配置。

## 权限必须贯穿查询和操作

- 总部相关人员可按授权查看全组织树及所属业务；所属单位用户只查看本单位授权范围，本级或含下级需明确配置。单位A含下级可覆盖A1、A2，不能看到单位B、C；系统管理角色不自动拥有全业务数据权限。
- 菜单和操作权限与业务数据范围分别控制。组织树、KPI分子分母、趋势、场景五数、列表、对象详情、规则事实、证据、导入导出、业务关系及AI均应用同一授权范围，不只隐藏菜单和树节点。
- 直接路由、复制链接、搜索对象ID、导出和跨域关联需再次检查授权。无权对象不得暴露名称、金额、证据或AI摘要；范围外关联仅按需求展示无敏感内容的提示。
- 用户切换或授权变化时，重设合法组织范围并清理旧用户抽屉、查询缓存、详情、AI上下文和不再授权的历史分析。不能在单位用户页面残留总部值或先显示越权内容再异步清除。
- 前端身份选择用于验证权限产品行为，当前身份和可看范围可理解地展示；不得宣称具备真实后端鉴权、生产登录或单点登录。

## 业务文案与数据性质

- 产品标题使用“海油工程｜穿透式监管平台”。业务界面采用“一级监管场景、监管子场景、场景描述、管控要求、监控指标、监测规则、执行方式、规则依据、计算依据、来源记录”等语义标签。
- 不在标题、字段、按钮、导出和提示中出现“附件B列”等Excel位置、“P71—P79”等内部页面编号、原始JSON字段名或“演示”字样。Excel原文、行列及`original_columns`仅保留在研发审计元数据，不能直接用来生成用户字段标题。
- 匿名主体统一为“下属二级单位A/B/C”“下属三级单位A1”“境外分支机构A2”等。名称变化不改ID及关联；项目与资产用合理业务名称。重置入口分别称“重置业务办理状态”“重置配置”，两者独立确认；不称“重置演示数据”。
- 集中“数据说明”明确当前是合成样例、用于功能验证、不代表海油工程真实经营数据；真实历史事件与样例价格区分。去掉业务名称中的“演示”不等于隐藏样例属性或冒充真实数据。
- 以规范中央企业正常管控为背景，异常样例保持为管理偏差或待核查线索。不能通过修改数字消除原8件基准，也不能把线索直接描述为已确认违法违规或实际损失。

## 规则能力与执行状态

- 原53项子场景及38项指标完整保留。旧40项结构化、10项线索、3项专业标签仅供历史追溯，不代表40项都可自动运行。按V1.4 catalog逐项采用结构化自动识别、线索识别与人工复核、专业核查支撑；AI是否辅助由具体材料和任务决定，不将线索识别统一等同于AI自动运行。
- 人工或专业判断仍可通过材料关联、任务分派、核查记录、结论及整改在线管理，不因无法自动判断而删掉业务监管场景。AI辅助不能替代签署审批、专业意见或责任认定。
- 对每个对象×规则实例分别保存业务适用性、数据/材料准备情况和执行结论。无业务、不适用、待接入、缺具体字段/材料、待人工核查、无异常、发现线索分别显示；不得统一写“数据不足”或把未核查当作正常。
- 首页重点KPI只显示输入可靠且解释充分的指标。其余定义保留在配置目录，数据页列明需要的字段、来源和准备工作；不伪造完整监测覆盖率。
- 既有自动评估记录仍按事实和当时版本留存，不因配置重新分类或新增待核查对象而抹去有效命中，也不因规则定义存在就生成评估结果。

## AI上下文分析

- 右下角“AI分析”入口打开侧边面板，不遮挡分页、表格操作、提交及弹窗关闭。按当前领域、指标、对象、子场景或规则携带组织授权、期间、截至日、定义与配置版本、数据版本及证据。
- 预设任务包含指标解释、命中依据梳理、材料差异对比和核查问题建议；输出定位到实际已有样例事实或材料位置，区分事实、规则判断、AI建议和待人工确认。
- 未连接真实模型时面板清楚标注“预置分析”，设置及说明中写明未连接模型服务。只对有配置和事实支撑的任务输出，不把任意提问装成实时模型回复，不伪造来源、法规条文或专业结论。
- AI复用完整数据权限，身份变化清理此前上下文；不自动发布规则、关闭事项、执行付款等源系统动作，不直接认定违法违规、估值或减值。

## 指标计算依据防串用

- 指标值与其名称、定义、公式、输入、分子分母、结果、目标及证据绑定同一查询。至少区分`indicatorId + orgNode + period + asOf + definitionVersion + configVersion + dataVersion + authScope`，明细还需稳定对象ID。
- 不能只按对象套固定依据文本，不能投资计划执行率打开EAC偏差公式；组织/期间/身份/发布配置变化后，值与依据同步更新，旧缓存失效。
- 总部投资计划执行率为8400÷10000＝84%，FA-P001为4800÷6000＝80%；FA-P001预计完工投资偏差率为（11800－10000）÷10000＝18%。三项各自展示正确公式、输入来源及结果。

## 统一数据与关联

- 页面共用V1.4的demo_seed、catalog、configuration_seed初始化的数据与配置服务；业务事实和已确认阶段映射保持。读取、授权、计算、规则、版本、视图和持久化分离，不在每个领域另造mock数组。
- 名称只用于展示，关系使用稳定ID。沿seed的外键和业务关系连接对象；来源缺失、关系不完整时显示具体缺口。
- 管理组织树与法人、产权关系分开。二级单位直管项目跳过不存在的三级；被投企业不自动成为管理组织下级。
- 所有筛选、详情跳转和返回传递组织、期间、截至日、对象及来源上下文。返回恢复筛选、页码和位置。
- 固定资产项目与对外履约工程分别建档；项目到资产、股权到被投企业及资金义务、工程到合同成本付款、国际事件到敞口对象等关系能双向查看实际已有连接。
- 同一风险跨领域使用同一`risk_id`，同一对象多入口打开同一档案。关联次数不用于全局去重事项数或重复叠加业务金额。
- 场景主阶段用`primary_phase_id`，关联阶段用`associated_phase_ids`；阶段映射沿已确认模板和本版对应关系，保留旧版本来源及迁移依据；风险阶段/专题按seed关联，显示关系来源。
- 展示seed提供的数据来源、业务记录、时间、规则版本及证据位置；不得把模拟来源说成已连接真实系统。
- 首次复制种子到共用本地状态，升级保留有效已有办理记录和配置；刷新保留修改，保存失败有提示。“重置业务办理状态”只恢复办理记录和状态，配置保留；“重置配置”按确认范围恢复本包初始配置。两个动作分别说明影响并确认。

## 不可改动的基准口径

- 原投资目录：34项固定资产、19项股权，共53项；原KRI定义40次去重为38项。旧40/10/3标签只作为历史追溯，当前执行模式读V1.4适配。新增补充设计单独标识，不重算原53/38。
- 业务截至日固定2026-06-30，发生额默认2026-01-01至2026-06-30。不得以电脑当天日期自动改变演示逾期。
- 首版截至日仅开放已有完整快照2026-06-30；期间按已有事实选择，历史事件窗口独立。监管操作业务生效日期默认演示截至日，实际时间另记`recorded_at`，同日用`sequence`排序，`closed_at`按演示业务日期记录。未来其他快照须重建对应时点状态和余额，不能用当前status冒充历史。
- 期间切换按评估/观察记录判断命中，不能只因`first_detected`不在期间就筛掉对象；AC64不强制开放任意历史截至日。
- 恢复种子后未关闭8件：红4、黄4、待核查5、整改中3、逾期整改1。已排除R09不计当前风险；六领域15次显示去重为8件。
- 指标必须从基础记录计算，`expected_results`只用于验收。比例按分子合计/分母合计，不能直接平均百分比。
- 总部固定资产执行率84%，FA-P001为80%，FA-P003为90%；FA-P001预计完工11800、偏差18%，五项成本构成不得重叠。投资完成4800与资金支付4200分开。
- AS001连续两个完整季度48%、46%对比演示阈值50%；月度47%、45%、40%辅助展示。总体利用率60%仍可包含AS001异常。
- EQ-P001会计收益200与同一分红200不叠加；现金到账80另列。出资履约80%、缺口600；分红回收40%、未收120。
- 钢材+10%、运费+25%产生情景增量148，毛利率11.26%；基础毛利率12%不覆盖，原钢材及运费成本不重复添加。
- P-PAY001实付1200、有效批准800、业务可支付上限2000，仅超批准400。产权60%/60%/55%按同有效期核对，显示待核实。
- 账户余额6632、受限314.4、可用6317.6，原币与模拟汇率可查。
- 0、null、无业务、不适用、待接入、缺少特定数据/材料、待人工核查和已执行结论分开。未知不补0；必要事实不足说明具体缺口、所需来源及配置入口。年度/季度/月度和金额/余额口径不能混用。
- 阈值、方向和边界按业需与适配配置；底稿来源、待确认边界及演示参数分别标识。不通过修改种子或期望值掩盖计算错误。

## 核查整改

- 状态依次支持待核查、核查中、整改中、待复核、已关闭；核查可排除，复核可退回整改，新证据重开保留历史。
- 认领、核查结论、措施责任、整改完成说明、证据及复核结果遵守业需必填要求。只有复核通过才关闭，指标恢复或源系统变化不自动关闭事项。
- 逾期整改数仅统计已进入整改责任范围、截至日仍未关闭（含待复核）且超过有效整改截止日的risk_id；待复核不自动停表。核查/复核等其他办理SLA超期在工作台单列，不混入第五数；各阶段期限及调整历史保留，不改日期抹去历史超期。
- 任一领域办理后各关联领域及总览同步。R07复核通过后全局由8变7，资金、工程、国际化各减少1件。
- 关联业务金额、异常差额、未回收金额、情景成本增量分别命名；未经确认不得称为已造成损失或违规事实。

## 真实性与范围标识

- 主体、人员、金额、业务记录、行情、汇率及分析的合成样例属性通过统一数据说明清楚表达；业务界面使用规范匿名命名，不在每个名称后机械追加“演示”。
- 真实历史事件日期可作为参考，但与模拟行情分开标识；历史重放不证明2026演示项目遭遇该事件，价格同向变化不自动给出因果结论。
- AI按上下文展示有事实和材料位置支持的预设样例提取、比对与核查建议。无真实调用时面板标注“预置分析”，说明中明确未连接模型服务，任意提问不伪装成即时分析；不伪造制度条文、专业结论或正式审批。
- 前端角色切换验证完整菜单、操作、查询、汇总、详情、导出、关系和AI范围，不仅验证按钮差异；不宣称生产后端安全权限。正式接口、实时行情、生产单点登录、真实AI和业务执行系统不在本轮默认范围。

## 验证和交付

- 原型功能及资料核对范围为AC01至AC111。原AC01至AC108保留，本轮新增AC109至AC111；本轮按改动检查新增和受影响项，未受影响的既有证据可复用，不能将旧版通过记录冒充新增配置或权限已通过。
- 运行实际项目，围绕计算、权限、状态、关系和版本生效进行必要验证，避免只验证实现本身或为低影响文案新增重复测试。边界测试与初始业务基准分离，完成后恢复正常初始状态。
- 实测侧栏底部配置六页可操作，保存/刷新/停用/校验或试算/发布与历史版本符合要求，专业核查模板不以自动结论作为发布门槛；主文明确的配置引用导入、恢复范围和保存失败状态可解释。
- 实测总部、单位A、单位B及系统管理角色的授权：菜单、汇总、明细、导出、关联、AI、复制链接及身份切换不越权；配置权限不自动带来全量业务权限。
- 保留受影响的指标树、阶段联动、五数去重、运营与后评价、历史阶段事项、跨域risk_id和整改复核路径。重点核对84%、80%、18%的公式与来源同步，不只确认弹窗能打开。
- 查看1440×900及当前窗口中七业务入口、配置六页、四流程首页、指标抽屉与AI面板截图，修正侧栏溢出、字色不清、遮挡和无法操作入口；规范匿名名称与业务字段，不漏出附件列号或P71等研发标题。
- AI入口和预设任务有证据且不越权，未连接实时模型明确标注；三类执行模式与实例三轴状态分开，人工核查不冒充数据不足。
- 如环境缺少必要验证能力，记录未验证项、原因及复现步骤，不编造测试、截图或可访问链接。录屏不是本轮必要交付，不因录制失败重复阻塞。
- 更新现有README、操作说明、验收记录及必要截图；实际命令依工程填写，不重建另一套文档与代码项目。
- 沿现有工程已授权预览方式启动或复用服务，提供用户可点击的当前有效完整网址或真实Preview入口。远程内部localhost不是用户浏览器访问地址；如受环境限制无法提供，准确说明阻塞及用户最少必要操作。
- 不为提供链接擅自创建新的外部隧道、对外部署或接入付费服务。已授权普通修复持续完成；正式对外发布、真实系统操作和生产接入按用户届时明确范围执行。


## V1.4配置契约补充

configuration_seed.json中的目录定义、执行绑定和历史版本分开处理。38项原投资指标均可查定义，4项提供完整参考绑定；其余从现有已验证计算器迁移，找不到才保留草稿，不覆盖已有正常卡片。规则执行必须读取结构化condition和当前版本parameters，不按rule_id保留写死阈值。专业模板通过材料/责任/适用条件校验发布，不要求先得出专业结论。

使用coverage_migration重建旧覆盖候选的显示状态：31条实际监测关联按事实进入计数，其余358条是适用性尚待确认的覆盖规划候选，不能把旧data_insufficient批量渲染为数据不足。候选未经确认不进应评估分母；本期无适用对象是场景汇总空态。

definition_version_archive保留原观察版本，当前配置不能覆盖历史；resource_access_bindings提供源记录及证据的明确授权对象链，无归属默认拒绝。配置域权限与业务域权限独立，防止通过修改本人角色间接扩权。切换用户、配置或数据版本必须清理受影响缓存。
